# FRT 便捷功能批次——需求说明与验收标准

> 需求分析师产出，作为实现（t2）/ 验证（t3）/ 审查（t4）/ 集成（t5）的共同依据。
> 版本：v1（首版）　日期：以 git 提交为准　状态：已与工作区现有代码对齐（2026-08-30 复核）

## 1. 批次目标

为 FRT 图形界面（`--ui`，主窗口 `FRTFrame`）增加 4 项便捷功能，提升日常使用体验：

| 编号 | 功能 | 一句话描述 |
|------|------|-----------|
| FR-1 | 检查更新 | 查询 GitHub 最新 Release，与当前版本对比，有新版时提示并一键打开下载页 |
| FR-2 | 一键打开目录 | 菜单一键打开 更新/目标/删除/备份/日志 目录（不存在自动创建） |
| FR-3 | 关于对话框 | 程序名/版本号/构建时间/GitHub 链接/许可证 集中展示 |
| FR-4 | 深浅主题切换 | 浅色/深色主题即时切换，选择持久化到 config.json，重启保留 |

**范围限定**：仅图形界面（`MainUI` → `FRTFrame`）提供；控制台模式（`Main.java` 菜单）**不新增**任何功能，不回归。**不做**自动下载/安装更新，只做"对比 + 提示 + 打开下载页"；**不做**启动时自动检查更新（避免启动依赖网络），由用户手动触发。

## 2. 现状盘点（工作区已有代码，先读后写，避免需求与实现脱节）

### 2.1 船长已预写、未编译验证的文件（已复核：`mvn -o compile` 通过）

| 文件 | 已含内容 | 备注 |
|------|---------|------|
| `src/main/java/com/awei/frt/ui/UITheme.java` | 浅/深两套配色、`apply(boolean)`、`isDark()`、按钮样式、日志字体平台候选 | 颜色字段非 final，`apply()` 写入 UIManager 默认值；**注意 `apply()` 无参版本用静态 `dark` 标志（默认 false=浅色）** |
| `src/main/java/com/awei/frt/service/UpdateChecker.java` | `ReleaseInfo` record、`latestReleaseApiUrl()`、`fetchLatestRelease()`（5s 超时，异常返回 null）、`isNewer(tag, version)` 纯函数、`versionSegments()` | 网络失败一律 null/false 静默降级；API 地址由 `BuildInfo.GITHUB_URL` 推导 |
| `src/main/java/com/awei/frt/ui/AboutDialog.java` | 模态关于对话框：程序名+版本、构建时间（空显示"未知"）、许可证 MIT、GitHub 链接（LinkLabel） | 数据全部取自 `BuildInfo` |
| `src/main/java/com/awei/frt/ui/DesktopUtil.java` | `openDirectory(Path)`（不存在先创建）、`openPath`、`openUri`；Desktop API → xdg-open → 记日志 | 全失败仅记日志不崩溃 |
| `src/main/java/com/awei/frt/model/Config.java` | `theme` 字段（默认 `"light"`）、`THEME_LIGHT/THEME_DARK`、`getTheme()/setTheme()`（未知值回退浅色） | 已有 `logFontSize` 同款持久化先例 |
| `src/main/java/com/awei/frt/core/builder/ConfigLoader.java` | `saveTheme(String)` + `saveThemeTo(Path, String)`（合并写入 config.json，保留其他键，失败仅记日志） | 与 `saveLogFontSizeTo` 同款实现 |

### 2.2 本批次待办（需求对应）

1. **FRTFrame 菜单栏接线**（FR-0，见 §3.1）：菜单 → 打开目录/主题/检查更新/关于；
2. **主题接线**（FR-4）：FRTFrame 启动按 `config.getTheme()` 应用主题 + 切换时 `refreshTheme()` 即时重刷 + `ConfigLoader.saveTheme` 持久化；
3. **UpdateCheckerTest**（§7.2）：`isNewer`/`versionSegments`/`latestReleaseApiUrl` 纯逻辑全覆盖；
4. 整体编译与全量测试、审查、本地提交（由 t3/t4/t5 承接）。

## 3. 功能需求

### FR-0 菜单栏接线（本批次 UI 骨架）

`FRTFrame` 增加 `JMenuBar`，三项菜单，布局与文案如下（文案以最终实现为准，但菜单名与项名按此约定）：

- **文件**：`打开目录 ▸ 更新目录 / 目标目录 / 删除目录 / 备份目录 / 日志目录`，分隔线后 `退出`
- **视图**：`主题 ▸ 浅色 / 深色`（当前主题项带勾选标记），`日志字体 ▸ 缩小(A-) / 放大(A+)`（与顶部 A-/A+ 按钮同一行为，可选）
- **帮助**：`检查更新`、`关于`

约束：
- 菜单项复用既有动作（如 A-/A+ 复用 `adjustLogFontSize`）；菜单栏与菜单项使用 `UITheme` 已配置的 Menu/MenuItem/MenuBar 颜色键，深色主题下可读；
- 执行类动作（检查更新/打开目录/关于）必须可用且与顶部按钮同一行为风格：日志区输出 `[成功]/[失败]/[警告]` 标记、状态栏同步（复用 `appendText`/`statusLabel`）；
- 菜单触发不改变既有顶部按钮区布局与行为。

### FR-1 检查更新（帮助 → 检查更新）

**输入**：无（点击菜单项即触发）。

**处理流程**：
1. 后台线程（`SwingWorker`，不得阻塞 EDT）调用 `UpdateChecker.fetchLatestRelease()`；
2. 结果为 null（网络失败/API 不可达/`GITHUB_URL` 未配置）→ 弹提示："检查更新失败（网络不可用或 GitHub 无法访问），请稍后重试"，日志区记 `[警告]`，**不崩溃、不影响程序**；
3. 结果非 null → `UpdateChecker.isNewer(tagName, BuildInfo.VERSION)`：
   - `true`（有新版）→ 弹提示框，内容含：当前版本、最新版本（tag/名称/发布时间），按钮「打开下载页」（→ `DesktopUtil.openUri(htmlUrl)`，失败弹提示不崩溃）与「取消」；
   - `false`（无新版）→ 弹提示"当前已是最新版本"；
4. 版本对比语义（已由 `UpdateChecker.isNewer` 实现，测试锁定，不得改动语义）：
   - `v0.1.8` vs `0.1.7-SNAPSHOT` → **true**（有新版）；
   - `v0.1.7` vs `0.1.7-SNAPSHOT` → **false**（开发中版本与最新发布视为相同，不算旧）；
   - `v0.1.6` vs `0.1.7-SNAPSHOT` → **false**（当前开发版更新）。

**成功标准**：以上三条分支（有新版/无新版/失败）各自可触发且提示文案清晰；任何分支都不抛未捕获异常。

### FR-2 一键打开目录（文件 → 打开目录）

**输入**：菜单选择目标目录。

**处理流程**：
1. 路径来源：
   - 更新目录/目标目录/删除目录/备份目录 → `ConfigLoader.getUpdatePath()/getTargetPath()/getDeletePath()/getBackupPath()`（绝对路径静态量）；
   - 日志目录 → `Path.of("logs")`（logback FILE appender 的 `logs/frt.log` 所在目录，相对工作目录）；
2. 调 `DesktopUtil.openDirectory(path)`：目录不存在自动创建后打开（首次使用直达）；成功静默；
3. 失败（headless/无 xdg-open/IO 异常）→ `LoggerUtil` 已记日志，UI 弹一次性提示（如 JOptionPane 警告"无法打开目录：xxx，详见日志"），不崩溃。

**成功标准**：5 个目录项各自可打开；日志目录不存在时能自动创建；失败路径有用户可见提示且程序继续运行。

### FR-3 关于对话框（帮助 → 关于）

**输入**：无。

**处理流程**：`new AboutDialog(this).setVisible(true)`（模态、居中于主窗口）。内容（全部与主窗口同源，取自 `BuildInfo`）：
- 程序名 + 版本号（`BuildInfo.VERSION`，与标题栏/状态栏一致）；
- 构建时间（`BuildInfo.BUILD_TIME`，为空显示"未知"）；
- GitHub 链接（`LinkLabel` 可点击打开 `BuildInfo.GITHUB_URL`）；
- 许可证：MIT；
- 运行环境（Java 版本）。

**成功标准**：对话框可打开/关闭；链接可点击；深色主题下可读（已用 `UITheme` 颜色）。

### FR-4 深浅主题切换（视图 → 主题）

**启动时**：
1. `FRTFrame` 构造按 `config.getTheme()` 决定初始主题：`"dark"` → `UITheme.apply(true)`，否则 `UITheme.apply(false)`（浅色默认）；
2. 注意构造顺序：config 读取目前发生在组件创建之后（`UITheme.apply()` 在组件创建前、无参调用），实现需保证初始主题在窗口显示前生效——允许调整顺序，或在读取 config 后补一次 `apply(configTheme)` + `refreshTheme()`（此时窗口未显示，无闪烁）。

**切换时**：
1. 菜单点选「浅色/深色」→ `UITheme.apply(boolean)`；
2. 调 `FRTFrame.refreshTheme()`：`SwingUtilities.updateComponentTreeUI(this)` 刷新 UIManager 默认值捕获的组件，**并显式重刷直接 setBackground/setForeground 捕获旧色的组件**——至少包括：日志区（`logArea`/`logScroll` 视口与滚动条区）、快捷按钮滚动区（`quickScroll`/`quickPanel`）、状态栏（`statusLabel`/`statusBar`）、顶部按钮（`topButtons` 逐个重 style）——确保颜色无残留、无白块；
3. 持久化：`config.setTheme(...)` + `ConfigLoader.saveTheme("light"/"dark")`（合并写入 config.json，保留其他键，失败仅记日志，下次启动仍可用旧值）；
4. 未知值回退：`Config.getTheme()` 已保证非 light 即 dark 语义。

**成功标准**：切换即时生效且界面无残留旧色；`config.json` 的 `theme` 字段被合并更新（其他键保留）；重启后按 config 主题启动。

## 4. 交互与文案约定

- 提示/日志沿用既有风格：日志区 `[成功]` 绿 / `[失败]` 红 / `[警告]` 橙；状态栏显示简短结果；
- 弹窗用 `JOptionPane`（深色主题下 OptionPane 颜色键已由 `UITheme.applyUIManager` 配置）；
- 所有文案中文，语气与既有 UI 一致（参考顶部按钮/状态栏现有文案）。

## 5. 非功能需求（NFR）

1. **不阻塞 EDT**：网络操作（检查更新）必须在后台线程；任何弹窗在 EDT 上是模态可接受，但网络等待不得在 EDT 上发生；
2. **全链路静默降级**：网络失败、Desktop 不可用、IO 异常一律不崩溃、不影响既有功能；失败有日志（`LoggerUtil.logException/logWarn`）与用户可见提示；
3. **零新增依赖**：只用已有依赖（Jackson 等），不新增第三方库；
4. **Java 17 兼容**：编译目标 17，无平台特有 API（Desktop/xdg-open 回退已覆盖 Win/macOS/Linux）；
5. **既有功能零回归**：顶部按钮、日志区着色、快捷按钮（QuickOptions）、A-/A+ 字体持久化、向导弹窗、核心配置、备份恢复均不得受影响；
6. **可测试性**：纯逻辑（版本对比、API URL 推导、主题保存、UITheme 颜色）必须可无头测试；headless 下不可测的（真实打开目录/弹窗/菜单渲染）以静态核对 + 代码审查覆盖。

## 6. 实现约束（t2 必须遵守）

1. **不改** `UpdateChecker` 的 `isNewer`/`versionSegments` 语义（测试将锁定）；如需微调须在需求评审中提出；
2. **不改** `Config.getTheme/setTheme`、`ConfigLoader.saveThemeTo` 的语义与签名（含包可见的 `saveThemeTo(Path, String)` 测试钩子）；
3. `UITheme` 颜色字段非 final 是本批次的设计（切换主题后组件读到新值）；`apply()` 无参版本语义保留（沿用静态 dark 标志），启动主题应用走 `apply(boolean)`；
4. `refreshTheme()` 作为 `FRTFrame` 方法实现（包内可见即可）；被直接捕获旧色的组件必须显式重刷（见 FR-4），不能只依赖 `updateComponentTreeUI`；
5. 菜单项动作与既有 `runXxx` 风格一致：耗时动作走 `runService`/`runServiceWithProgress` 或独立 SwingWorker，弹窗动作在 EDT 直接执行；
6. 日志目录固定 `logs/`（与 logback.xml 一致），不要从 config.json 的 `logPath` 读（该键是旧字段，LoggerUtil 实际使用 logback 配置）；
7. 新增测试遵循既有模式：纯逻辑测试放对应包（参考 `LogFontSizeSaveTest`、`FRTFrameQuickOptionsTest`），用 `@TempDir` 隔离 config.json 写入污染；
8. 不触碰 `.agent-teams/`、`.dsh-*` 等运行时目录；不改 `pom.xml`（版本号/GitHub 地址已就绪：0.1.7-SNAPSHOT / https://github.com/GatherMemories/FRT）。

## 7. 验收标准（t3 逐条执行；t4 审查引用）

### AC-1 编译与打包
- [ ] AC-1.1 `mvn -o compile` 通过（main 全量，含预写 6 文件 + 菜单接线）；
- [ ] AC-1.2 `mvn -o test` 全绿（既有全部测试 + 本批次新增测试）；
- [ ] AC-1.3 `mvn -o package -DskipTests` 成功产出可执行 jar。

### AC-2 检查更新逻辑（UpdateCheckerTest，纯逻辑）
- [ ] AC-2.1 `latestReleaseApiUrl()`：`https://github.com/GatherMemories/FRT` → `https://api.github.com/repos/GatherMemories/FRT/releases/latest`；null/空白 → null；
- [ ] AC-2.2 `isNewer("v0.1.8","0.1.7-SNAPSHOT")` == true；`isNewer("v0.1.7","0.1.7-SNAPSHOT")` == false；`isNewer("v0.1.6","0.1.7-SNAPSHOT")` == false；
- [ ] AC-2.3 逐段数字比较：`isNewer("v1.10","v1.9")` == true；`isNewer("v0.1.10","v0.1.9")` == true；`isNewer("v0.1.9","v0.1.10")` == false；
- [ ] AC-2.4 边界容错：`isNewer(null, ...)` / `isNewer("v0.1.8", null)` / 非数字段 均不抛异常、语义合理；
- [ ] AC-2.5 `fetchLatestRelease()` 在无网络/坏地址下返回 null（不抛异常、不超 5s+ 明显延迟）。

### AC-3 主题持久化（Config + ConfigLoader）
- [ ] AC-3.1 新 `Config().getTheme()` 默认 `"light"`；`setTheme("dark")` → dark；`setTheme("xxx")`/null → light；
- [ ] AC-3.2 `saveThemeTo(tempDir/config.json, "dark")`：合并写入——theme=dark 且其他键（logLevel/updatePath 等）保留；
- [ ] AC-3.3 `saveThemeTo` 文件不存在时自动创建；
- [ ] AC-3.4 保存失败（如只读路径）不抛异常（仅记日志）——测试可用不可写场景或静态核对。

### AC-4 UITheme 主题切换（纯逻辑）
- [ ] AC-4.1 `apply(true)` 后：`isDark()`==true、`UITheme.BG`==0x111827、`TEXT`==0xE5E7EB、`LOG_BG`==0x111827（深色值）；`apply(false)` 后回浅色值；
- [ ] AC-4.2 任意时刻所有颜色字段非 null（未调用 apply 前也不为 null）；
- [ ] AC-4.3 `apply(true)` 与 `apply(false)` 的 BG/TEXT/LOG_BG/LOG_TEXT 等关键颜色互不相同。

### AC-5 FRTFrame 菜单结构（静态核对为主）
- [ ] AC-5.1 `FRTFrame` 存在 `JMenuBar`：文件/视图/帮助 三项菜单；
- [ ] AC-5.2 文件菜单含 打开目录（更新/目标/删除/备份/日志 5 项）与 退出；
- [ ] AC-5.3 视图菜单含 主题（浅色/深色，当前项勾选）与 日志字体（A-/A+，可选）；
- [ ] AC-5.4 帮助菜单含 检查更新 与 关于；
- [ ] AC-5.5 主题菜单项勾选状态与 `UITheme.isDark()`/config 主题一致（切换后更新）。

### AC-6 主题切换行为
- [ ] AC-6.1 启动：config.json `theme:"dark"` 时窗口以深色启动（`UITheme.apply(true)` 生效）；
- [ ] AC-6.2 切换：点「深色」后日志区/状态栏/快捷区/顶部按钮颜色即时变化，无残留浅色块；
- [ ] AC-6.3 切换后 config.json 的 `theme` 字段被合并更新（其他键保留）；
- [ ] AC-6.4 重启后按 config 记录的主题启动。

### AC-7 检查更新行为（可手工/静态核对部分）
- [ ] AC-7.1 帮助 → 检查更新：无网络时提示失败且程序不崩溃（可断网/坏 GITHUB_URL 场景核对，或由实现注入可测路径）；
- [ ] AC-7.2 有新版分支：提示含当前版本与最新版本，提供「打开下载页」（DesktopUtil.openUri）与「取消」；
- [ ] AC-7.3 无新版分支：提示"当前已是最新版本"；
- [ ] AC-7.4 检查更新不阻塞 UI（后台线程）。

### AC-8 打开目录行为
- [ ] AC-8.1 5 个目录项调用 `DesktopUtil.openDirectory`，路径与 ConfigLoader 绝对路径一致；日志目录为 `logs/`；
- [ ] AC-8.2 目录不存在时自动创建后打开（可用临时目录静态核对/单元验证）；
- [ ] AC-8.3 打开失败路径有用户提示与日志，不崩溃（headless 下 `DesktopUtil` 返回 false 不抛异常）。

### AC-9 关于对话框
- [ ] AC-9.1 帮助 → 关于 打开模态对话框，含 程序名+版本 / 构建时间（空→"未知"）/ GitHub 链接（可点击）/ 许可证 MIT；
- [ ] AC-9.2 版本号与 `BuildInfo.VERSION` 一致（与标题栏同源）；
- [ ] AC-9.3 可关闭，深色主题下可读。

### AC-10 静态核对（审查官/验证工程师）
- [ ] AC-10.1 无新增第三方依赖（pom.xml 未改动）；
- [ ] AC-10.2 无 `System.exit`、无 EDT 上网络等待、无未捕获异常路径（网络/Desktop/IO 全降级）；
- [ ] AC-10.3 未改动控制台模式（`Main.java` 无 diff 或仅注释级）；
- [ ] AC-10.4 新增/改动文件均有中文注释与既有风格一致；`pom.xml`、`BuildInfo` 未改。

## 8. 测试需求（t2 实现，t3 执行）

1. **`UpdateCheckerTest`**（新增，`src/test/java/com/awei/frt/service/`）：覆盖 AC-2 全部纯逻辑；`fetchLatestRelease` 网络路径仅验证"失败返回 null"（不依赖真实网络，可指向不可达地址或空 GITHUB_URL）。
2. **主题保存测试**（新增，参照 `LogFontSizeSaveTest` 模式，`core/builder` 包）：AC-3 全部；`Config.getTheme/setTheme` 语义。
3. **UITheme 测试**（新增或并入）：AC-4 全部（纯静态，无 headless 问题）。
4. **菜单结构**：headless 下无法构造 JFrame 弹窗，不强制自动化；AC-5 由 t3 静态核对源码 + t4 审查确认。若实现把"菜单构建"抽成静态方法（如 `buildMenuBar()` 返回 JMenuBar 供构造调用），可对该方法做 headless 单元测试（构造 JMenuBar 不需要显示环境）——推荐但不强制。
5. **既有测试零回归**：`mvn -o test` 全绿（现有 ~30 个测试类）。

## 9. 风险与回归关注点（t4 审查重点）

1. **主题切换颜色残留**：`updateComponentTreeUI` 不刷新直接 set 颜色的组件——`refreshTheme()` 遗漏任一组件都会留浅色块（用户可见）。审查逐组件核对（日志区/滚动条/状态栏/快捷区/顶部按钮/菜单栏本身）。
2. **构造顺序**：`UITheme.apply()` 在 FRTFrame 组件创建前无参调用（浅色默认）；按 config 启动深色时若顺序不当，窗口先浅后深或深色不生效。
3. **`isNewer` 语义被测试锁定**：若实现侧误改，AC-2 会立刻红；不要"顺手优化"。
4. **saveTheme 与 saveLogFontSize 并发写**：两者都是"读-改-写"整文件，用户快速连续操作时理论上可能互相覆盖——本批次不要求并发保护（与既有 logFontSize 行为一致），但审查需确认无新增并发入口。
5. **日志目录打开**：`logs/` 是相对工作目录；发布包（zip）场景下工作目录即解压目录，正常。不引入 config `logPath` 键，避免两套真相。
6. **菜单栏深色可读性**：`JMenuBar` 组件背景默认可能不透传 UITheme 颜色，需显式设置（UIManager 键已配，但个别 L&F 下要组件级 set）。

## 10. 交付物（t5 集成范围）

- 本需求文档（`需求-便捷功能批次.md`）；
- 代码：FRTFrame 菜单栏接线 + `refreshTheme()`、主题启动/切换/持久化接线、检查更新/打开目录/关于的菜单动作、`UpdateCheckerTest` 及主题/UITheme 测试；
- 提交信息建议：`feat(ui): 便捷功能批次——菜单栏/检查更新/打开目录/关于/深浅主题`（最终由船长定夺，本地提交到 dev，不推送）。
