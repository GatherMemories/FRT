1. 移除Gson依赖，改用jackson，json转化
2. 我的世界模组元数据获取 BasicModInfoParser-2.0.0.jar，还有些问题：
比如引用内容 {xx.xx} ，会导致，解析失败，需要改进
3. 去除 “策略工厂” 的 StrategyType，枚举，让每个策略类自己储存 StrategyType属性




=========功能升级项（优先级低）==========
1. 让匹配规则文件，支持多种策略组合（比如执行流程 策略A--> 策略B(执行匹配，策略A剩下的文件) --> 策略C(执行匹配，策略B剩下的文件)）
2. 加载外部策略类，实现动态加载（让玩家不需要修改源码，可以自己按规范，编写匹配规则的策略类）（策略类型属性，需要写在策略类里，方便记录key值）

=========持续改进点/大致次要改进=========
1. 重审代码，改进bug点（安全漏洞）
2. 项目重构优化（框架--精简化）
3. UI优化（美化）
4. 代码优化（性能）
