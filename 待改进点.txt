//1. 移除Gson依赖，改用jackson，json转化

//2. 我的世界模组元数据获取（已自研 ModMetadataParser 替代 BasicModInfoParser-2.0.0.jar：
//   支持 NeoForge/Forge/Fabric/Quilt/旧版mcmod.info，版本占位符自动兜底 MANIFEST→文件名）

//3. 去除 "策略工厂" 的 StrategyType 枚举 ✅（2026-08-19）
//   已改为注册表：每个策略类自报 getStrategyType()，StrategyFactory.register 登记，
//   配合外部插件动态注册（见功能升级2）

//4. 基础文件夹创建优化

//5. 文件过滤器（动态代理）✅（2026-08-19）
//   已落地 StrategyProxy：统一 执行日志/耗时/异常兜底（记日志+记失败统计，不中断更新）
//   handler/ 下的旧 txt 草案已删除

//6. 抛出异常统一处理记录到日志（已统一 LoggerUtil.logException，printStackTrace 全清零）

//7. 策略 execute 简化 ✅（2026-08-19）
//   新增 AbstractOperationStrategy 模板方法：统一 null/类型校验 + add/replace/delete 钩子分派，
//   FileSameNameStrategy / McModStrategy 重复代码已消除（新增策略只需实现三个钩子）

//8. 日志功能（已使用 slf4j-api + logback）

//9. 递归部分改成迭代（核心遍历已栈化）

//10. 更新过程中异常关闭的情况，备份记录实时写入（session-current.json）

//-1. bat窗口使用jar，抛出异常捕获后程序直接终止（shade 全量打包 + catch Throwable 顶层兜底）

//11. 测试工具修复 ✅（2026-08-19）
//    surefire 2.17 → 3.2.5：修复 JUnit5 测试（@Test）从未真正运行的"疑难杂症"
//    （原先 surefire 2.17 只靠方法名以 test 开头的 JUnit3 启发式"假装"跑通 3 个，@Test 全部漏跑）

//12. 备份孤立文件清理入口 ✅（2026-08-19，P2）
//    主菜单 5：识别备份索引中未被任何操作记录引用的孤立文件，列表确认后删除；
//    另修 deleteBackupFile 索引值为 null 时的 NPE 防御

=========功能升级项==========

//1. 多策略组合链 ✅（2026-08-19）
//   规则文件支持 strategyChain：依次执行各策略步骤，后续策略只处理前序"剩余"文件（FileNode.handled 标记）
//   例：策略A(匹配*.txt) --> 策略B(匹配*.json，A剩下的文件) --> 策略C(...)
//   交互向导（菜单4）已支持链配置

//2. 外部策略动态加载 ✅（2026-08-19）
//   玩家无需改源码：plugins/ 目录 jar 扫描（标准 SPI 或自动类扫描）+ classpath SPI
//   策略类型写在策略类 getStrategyType() 里，规则文件直接引用

=========持续改进点/大致次要改进=========

1. 重审代码，改进bug点（安全漏洞）—— 本次已修：备份索引污染、glob 正则元字符、getBackupFiles 判空、System.err 35处收口、deleteBackupFile null 防御、历史备份记录 Windows 路径反序列化崩溃（用户实测发现）
2. 项目重构优化（框架--精简化）—— 已做：注册表去枚举/模板方法/服务层去重/死代码清理/向导树打印栈迭代/测试备份路径隔离；剩余：UI 迭代
3. UI优化（美化）—— ✅ 基础版（Swing --ui）；✅ 更新/删除 dry-run 预览二次确认；✅ 向导改名"规则生成"+结构图标注规则文件与受规则影响文件；剩余：向导表单化/进度条/美化
4. 代码优化（性能）—— ✅ 已做：MD5 LRU 缓存、模组元数据解析 LRU 缓存、session JSON 增量写（JSON Lines 逐条追加）
