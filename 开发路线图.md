# FRT 开发路线图（代码审查记录）

> 最后审查日期：2026-08-16
> 关联文件：`待改进点.txt`
> 本文档用于记录代码状态对照与后续开发计划，方便持续跟进。

---

## 一、待改进点.txt 状态对照

### ✅ 已完成

| 条目 | 说明 |
|---|---|
| 1. 移除 Gson 改用 Jackson | 项目代码已 100% Jackson；残留 import/注释/pom 污染已清理。⚠️ **注意**：Gson **依赖必须保留**——它是 BasicModInfoParser 的运行时依赖（曾因误删导致 NoClassDefFoundError 崩溃） |
| 4. 基础文件夹创建优化 | `ConfigLoader.validateAndEnsureDirectory` 自动创建 |
| 8. 日志功能 | 已完成 |
| 9. slf4j + logback | LoggerUtil + logback.xml |
| 10. 递归改迭代 | 核心遍历已栈化（FolderNode.process / buildChildren / printTree）。⚠️ 残留：`FileDeleteService.hasFilesToDelete` / `countFiles` 仍为递归（浅层，影响小，建议改成栈迭代统一风格） |
| 11. 备份记录实时写入 | `session-current.json` 实时落盘 + 启动时未完成会话恢复提示 |
| -1. bat 窗口 jar 崩溃 | shade 全量打包（含 Gson 回归），另已补 `catch Throwable` 顶层兜底 |

### ⚠️ 部分完成

| 条目 | 现状 | 缺口 |
|---|---|---|
| 6. 异常统一处理 | Main 顶层已 catch Throwable 并留日志 | 全项目仍有 **28 处 `e.printStackTrace()`**（BackupFileLoader 占 18 处），未收口到统一日志处理 |

### ❌ 未完成（后续开发重点）

| 条目 | 现状 | 建议方案 |
|---|---|---|
| 2. 模组版本 `${file.jarVersion}` 解析失败 | 真实 jar 解析出版本占位符字面量（BasicModInfoParser 不解析 gradle 占位符） | ① FRT 侧 fallback：解析失败/含 `${` 时从**文件名**提取版本；② 更换或修改外部库 |
| 3. 去策略工厂 StrategyType 枚举 | `StrategyFactory.StrategyType` enum + switch 仍在 | 接口加 `getStrategyType()`；工厂改注册表 `Map<String, Supplier<OperationStrategy>>`；**与功能升级 2 一起做** |
| 5. 动态代理过滤器 | `handler/*.txt` 仍是草案（未编译） | 落地为代理：统一处理 规则校验→日志→执行→统计；或先清理草案文件 |
| 7. 策略 execute 简化 | 两个策略 add/replace/delete 三段判断重复 | 抽象 `AbstractOperationStrategy` 模板方法（tryAdd/tryReplace/tryDelete 钩子） |
| 功能升级 1：多策略组合 | 未做 | 规则文件支持策略列表 + 层内串联，传递"剩余文件" |
| 功能升级 2：外部策略动态加载 | 未做 | `ServiceLoader` 或插件目录扫描（与 #3 / #7 联动） |

---

## 二、审查新发现的问题

### 🔴 P0（建议尽快修复）

1. **日志路径配置失效**：`config.json` 的 `logPath: ./testDic/logs` 实际无效——`logback.xml` 硬编码 `logs/frt.log`（相对工作目录），日志实际写在项目根 `logs/`。`ConfigLoader` 解析了 logsPath 但未接给 logback。
   - 方案：LoggerUtil 初始化时按 `config.logPath` 动态设置 logback 文件路径。
2. **`BackupFileLoader.getBackupFiles()` 判空条件写错**（约 49 行）：`operationRecordFiles == null || backupFiles.isEmpty()` 用错字段（应为 `backupFiles == null`），当前靠 backupFiles 初始非 null 才没出事。
3. **构建产物污染 git**：`dependency-reduced-pom.xml`（shade 生成物）被跟踪且每次打包变化 → 应加入 `.gitignore`。

### 🟡 P1（技术债）

4. **死代码**：`OperationContext.backup()` / `confirm()` 零调用（备份实际走 `FileUtil.replaceFile → BackupFileLoader.addBackupFile`）——删除，或接上"替换前确认"功能。
5. **glob 正则不严谨**：`FileSameNameStrategy.matches` 转正则时未转义 `[ ] ( )` 等元字符；`MatchRuleLoader.matches` 与策略内 `matches` 是两套重复实现，应合并。
6. **备份文件索引污染**：`loadBackupFiles` 会把 `backup/record/*.json` 也当备份文件算 MD5 入索引；新旧镜像目录并存。
7. **服务层重复**：`FileUpdateServiceNew` / `FileDeleteService` 的"备份+失败恢复询问"约 30 行几乎逐字重复，可提炼公共方法。
8. **空壳策略**：`ZipFileContentStrategy` / `ZipFileNameStrategy` 不实现 `OperationStrategy` 接口（留着误导）——应实现接口或删除。
9. **性能**：`getModInfo` 每次全量解 jar 解析（大 mods 目录慢）；`loadBackupFiles` 全量算 MD5；session 实时写整份 JSON（操作多时文件膨胀）。

---

## 三、后续开发路线

### P0（下一迭代，约半天）
- [ ] 日志路径配置接入（logPath 生效）
- [ ] 修正 `getBackupFiles()` 判空条件
- [ ] `.gitignore` 加 `dependency-reduced-pom.xml`
- [ ] 清理死代码（OperationContext.backup / confirm）
- [ ] `FileDeleteService` 递归改栈迭代（统一风格）

### P1（架构演进，核心投入）
- [ ] 策略注册表重构（去枚举，对应待改进点 3 + 功能升级 2）
- [ ] 模板方法简化策略 execute（待改进点 7）
- [ ] 多策略组合支持（功能升级 1）
- [ ] 版本解析 fallback（待改进点 2）

### P2（工程质量）
- [ ] 异常统一收口（替换 28 处 printStackTrace，待改进点 6）
- [ ] 服务层重复代码提炼
- [ ] handler 草案落地或清理（待改进点 5）
- [ ] 测试 JUnit 化补强（策略、备份恢复、规则向导）

### P3（体验 / 性能）
- [ ] UI 化（交互向导已铺路，可套 Swing / Web）
- [ ] MD5 / 模组解析缓存
- [ ] bat 启动脚本（chcp 65001 + java -jar）

---

## 四、本次会话已完成的历史修复（供追溯）

| 修复 | 位置 |
|---|---|
| McMod 删除/替换 NPE（targetModInfo 判空） | McModStrategy |
| 回滚操作类型字符串不匹配（"ADD" vs "operation_add"） | BackupFileLoader |
| FileUtil 空参数 NPE（MD5 计算移到校验后） | FileUtil |
| 备份同名覆盖（按相对路径镜像存储） | BackupFileLoader |
| REPLACE 恢复用错签名（sourceFileSign → targetFileSign） | BackupFileLoader |
| 空排除列表误杀所有文件（matches 空列表语义） | FileSameNameStrategy |
| ADD 不创建父目录 | FileUtil |
| 规则文件 BOM 解析失败 | MatchRuleLoader |
| 单 jar 解析失败崩整个更新（catch Throwable 兜底） | McModStrategy |
| Main 顶层 catch Throwable（Error 也留日志） | Main |
| Gson 回归（BasicModInfoParser 运行时依赖） | pom.xml |
| replacements 升级为 Map 策略扩展参数 + getRuleParam | MatchRule / OperationContext / 两策略 |
| 新增交互式规则配置向导（菜单 4） | RuleConfigWizard（新文件） |
