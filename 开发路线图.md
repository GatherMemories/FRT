# FRT 开发路线图（代码审查记录）

> 最后审查日期：2026-08-19
> 关联文件：`待改进点.txt`
> 本文档用于记录代码状态对照与后续开发计划，方便持续跟进。

---

## 一、待改进点.txt 状态对照

### ✅ 已完成（含 2026-08-19 收口的全部剩余项）

| 条目 | 说明 |
|---|---|
| 1. 移除 Gson 改用 Jackson | 项目代码 100% Jackson；Gson 依赖随 BasicModInfoParser 一并移除 |
| 2. 模组元数据获取（自研） | `ModMetadataParser`：NeoForge/Forge/Fabric/Quilt/旧版 mcmod.info；版本占位符兜底 MANIFEST→文件名 |
| 3. 去策略工厂 StrategyType 枚举 | **2026-08-19**：`StrategyFactory` 改注册表 `Map<String, Supplier<OperationStrategy>>`，策略类自报 `getStrategyType()`；枚举+switch 删除；规则校验改 `isSupported` |
| 4. 基础文件夹创建优化 | `ConfigLoader.validateAndEnsureDirectory` 自动创建 |
| 5. 文件过滤器（动态代理） | **2026-08-19**：`StrategyProxy`（JDK 动态代理）落地——统一 执行日志/耗时/异常兜底（记日志+记失败统计、不中断更新）；工厂返回代理对象；handler/ 旧 txt 草案删除 |
| 6. 异常统一处理 | `LoggerUtil.logException` 全项目收口，printStackTrace 清零 |
| 7. 策略 execute 简化 | **2026-08-19**：`AbstractOperationStrategy` 模板方法（accepts 过滤 + add/replace/delete 钩子 + stopOnHandled 语义），两个策略去重 |
| 8. 日志功能 | slf4j-api + logback |
| 9. 递归改迭代 | 核心遍历栈化（FolderNode.process/buildChildren、hasFilesToDelete/countFiles） |
| 10. 备份记录实时写入 | `session-current.json` 实时落盘 + 启动恢复提示 |
| 11. 测试工具修复（疑难杂症） | **2026-08-19**：surefire 2.17 → 3.2.5，JUnit5 `@Test` 从"从未运行"变为真实运行（测试数 3 → 35） |
| -1. bat 窗口 jar 崩溃 | shade 全量打包 + `catch Throwable` 顶层兜底 |

### ✅ 功能升级项（2026-08-19 全部完成）

| 条目 | 说明 |
|---|---|
| 1. 多策略组合链 | `MatchRule.strategyChain`：链中每个步骤是独立规则对象（type/patterns/exclude/replacements），依次执行；`FileNode.handled` 标记"剩余文件"语义；向导（菜单4）支持链配置；测试 `StrategyChainTest` |
| 2. 外部策略动态加载 | `StrategyLoader`：plugins/ 目录 jar 扫描（标准 SPI `META-INF/services` 或自动类扫描）+ classpath SPI；类型冲突内置优先；向导动态列出全部已注册策略；测试 `StrategyLoaderTest`（含运行时编译插件 jar 验证） |

---

## 二、审查新发现的问题

### 🔴 P0（已全部解决）

1. ~~日志路径配置失效~~ ✅ 删除 logPath 死配置，固定 logs/frt.log
2. ~~getBackupFiles 判空条件写错~~ ✅ 已修 + 回归测试
3. ~~构建产物污染 git~~ ✅ dependency-reduced-pom.xml 已 ignore
4. ~~JUnit5 测试从未真正运行~~ ✅ **2026-08-19**：surefire 2.17（JUnit3 启发式）→ 3.2.5（JUnit Platform），McModStrategyTest/ModMetadataParserTest 等 @Test 测试真实生效

### 🟡 P1/P2（2026-08-19 收口）

| 问题 | 处理 |
|---|---|
| glob 正则不严谨（未转义 [ ] ( ) 等元字符；两套重复实现） | ✅ 统一 `GlobMatcher`：`*`→`.*`、`?`→`.`、其余元字符转义；FileSameNameStrategy 与 MatchRuleLoader 共用；新增 `GlobMatcherTest` |
| 备份文件索引污染（backup/record/*.json 被当备份文件算 MD5 入索引） | ✅ `loadBackupFiles` 排除 record/ 子目录；新增回归测试 |
| 服务层重复（两个服务约 30 行备份+恢复询问重复） | ✅ 提炼 `BackupFileLoader.finishOperationSession`，两个服务共用 |
| 空壳策略（ZipFileContentStrategy / ZipFileNameStrategy 不实现接口） | ✅ 删除（规划功能可随时以插件接入） |
| 死代码 / 性能（getModInfo 全量解 jar、loadBackupFiles 全量 MD5、session 膨胀） | ✅ 部分：新增 MD5 LRU 缓存（FileSignUtil）与模组解析 LRU 缓存（McModStrategy）；session 增量写留作后续 |

---

## 三、后续开发路线

### P0（已完成，2026-08-16 ~ 2026-08-19）
- [x] 日志路径处理 / getBackupFiles 判空 / .gitignore 构建产物 / 死代码清理 / FileDeleteService 栈迭代
- [x] **surefire 3.2.5 修复 JUnit5 漏跑**（2026-08-19）

### P1（架构演进，2026-08-19 已完成）
- [x] 策略注册表重构（去枚举，待改进点 3）
- [x] 模板方法简化策略 execute（待改进点 7，AbstractOperationStrategy）
- [x] 多策略组合支持（功能升级 1，strategyChain）
- [x] 外部策略动态加载（功能升级 2，plugins/ + SPI）
- [x] 动态代理过滤器（待改进点 5，StrategyProxy）

### P2（工程质量）
- [x] 异常统一收口（printStackTrace 清零，LoggerUtil；2026-08-19 再收口 System.err 35处→logErrorMsg）
- [x] 服务层重复代码提炼（finishOperationSession）
- [x] handler 草案落地为代理（StrategyProxy）并清理 txt
- [x] 测试 JUnit 化补强（39 个测试：策略/链/插件/备份/清理/规则向导）
- [x] 死代码清理（FileUtil.executeOperation/executeOperations/createRecord、MatchRuleLoader.matches）
- [x] 测试修复（BackupFileLoaderTest 硬编码 Windows 路径→真实断言；FileUtilTest 空壳补 JUnit；清理测试初始化顺序疑难杂症）
- [x] 备份孤立文件清理入口（主菜单 5：识别未被记录引用的备份文件，确认后删除）

### P3（体验 / 性能）
- [x] UI 化基础版（Swing：--ui 启动，5 功能按钮+日志区+对话框交互；服务层交互抽象为 UserPrompter，控制台/Swing 双实现；剩余：界面美化/向导可视化表单/进度条迭代）
- [x] MD5 / 模组解析缓存（LRU，2026-08-19）
- [x] bat 启动脚本（start-frt.bat）
- [x] 规则向导文件树打印改栈迭代（原 printNode/printChildren 递归，超深目录栈溢出隐患）
- [x] session JSON 增量写（JSON Lines 逐条追加 + 旧格式兼容读取，2026-08-19）

---

## 四、本次会话已完成的历史修复（供追溯）

| 修复 | 位置 |
|---|---|
| McMod 删除/替换 NPE（targetModInfo 判空） | McModStrategy |
| 回滚操作类型字符串不匹配（"ADD" vs "operation_add"） | BackupFileLoader |
| FileUtil 空参数 NPE（MD5 计算移到校验后） | FileUtil |
| 备份同名覆盖（按相对路径镜像存储） | BackupFileLoader |
| REPLACE 恢复用错签名（sourceFileSign → targetFileSign） | BackupFileLoader |
| 空排除列表误杀所有文件（matches 空列表语义） | FileSameNameStrategy |
| ADD 不创建父目录 | FileUtil |
| 规则文件 BOM 解析失败 | MatchRuleLoader |
| 单 jar 解析失败崩整个更新（catch Throwable 兜底） | McModStrategy |
| Main 顶层 catch Throwable | Main |
| 自研模组元数据解析器（替代 BasicModInfoParser） | ModMetadataParser / ModInfo |
| 递归改栈迭代（hasFilesToDelete / countFiles） | FileDeleteService |
| 文件树图标 ASCII 化（修复 GBK 乱码） | FileTreeBuilder / RuleConfigWizard |
| 异常统一收口（printStackTrace 29 处清零） | LoggerUtil / 9 个文件 |
| **surefire 3.2.5 修复 JUnit5 漏跑**（2026-08-19） | pom.xml |
| **策略注册表去枚举 + 模板方法 + GlobMatcher**（2026-08-19） | StrategyFactory / AbstractOperationStrategy / GlobMatcher / 两策略 |
| **策略动态代理（日志/异常兜底/统计）**（2026-08-19） | StrategyProxy / StrategyFactory |
| **多策略组合链（handled 剩余文件语义）+ 向导链配置**（2026-08-19） | MatchRule / FileNode / FolderNode / FileLeaf / RuleConfigWizard |
| **外部策略动态加载（plugins/ + SPI + 自动类扫描）**（2026-08-19） | StrategyLoader / StrategyFactory |
| **备份索引排除 record 目录 / 服务层去重 / MD5与模组解析缓存 / 删空壳策略**（2026-08-19） | BackupFileLoader / FileSignUtil / McModStrategy / 两服务 |
| **System.err 35处收口为 LoggerUtil.logErrorMsg**（2026-08-19） | BackupFileLoader / MatchRuleLoader |
| **死代码清理 + 测试修复（硬编码路径/空壳/初始化顺序 NPE）**（2026-08-19） | FileUtil / MatchRuleLoader / BackupFileLoaderTest / FileUtilTest / BackupCleanupTest |
| **规则向导树打印递归改栈迭代**（2026-08-19） | RuleConfigWizard |
| **备份孤立文件清理入口（主菜单 5）+ deleteBackupFile null 防御**（2026-08-19） | BackupFileLoader / Main / BackupCleanupTest |
| **session-current.json 增量写（JSON Lines）+ 旧格式兼容**（2026-08-19，P3） | BackupFileLoader / OperationContext / SessionRecordTest |
| **Swing 基础版 UI + UserPrompter 交互抽象**（2026-08-19，P3） | ui/ 包（FRTFrame/MainUI/SwingPrompter/UserPrompter）/ 服务层 5 文件 / Main |
