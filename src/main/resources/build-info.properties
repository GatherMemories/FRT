# ============================================================
#  构建信息（自动生成）
#  Maven 打包时由 pom.xml 过滤注入占位符（见 pom.xml <resources>），
#  请勿在此手动修改版本号 —— 版本唯一来源是 pom.xml 的 <version>。
#  升级版本：改 pom.xml → 重新 mvn package → 界面/控制台自动显示新版本。
# ============================================================
app.name=${project.name}
app.artifactId=${project.artifactId}
app.version=${project.version}
app.githubUrl=${project.github.url}
app.buildTime=${maven.build.timestamp}
