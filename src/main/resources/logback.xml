<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 控制台输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder charset="UTF-8">
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 文件输出 -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/frt.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/frt.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder charset="UTF-8">
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 根日志配置 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

    <!-- FRT 包日志配置 -->
    <logger name="com.awei.frt" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </logger>

    <!-- 仅文件日志配置 - 不输出到控制台 -->
    <logger name="FILE_ONLY" level="DEBUG" additivity="false">
        <appender-ref ref="FILE"/>
    </logger>

    <!-- System.out 捕获配置 - 只输出到文件，避免循环 -->
    <logger name="System.out" level="INFO" additivity="false">
        <appender-ref ref="FILE"/>
    </logger>

    <!-- System.err 捕获配置 - 只输出到文件，避免循环 -->
    <logger name="System.err" level="ERROR" additivity="false">
        <appender-ref ref="FILE"/>
    </logger>
</configuration>
