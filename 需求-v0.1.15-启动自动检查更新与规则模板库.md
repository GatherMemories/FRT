# FRT v0.1.15 功能批次——需求说明与验收标准

> 需求分析师产出，作为实现（t2）/ 验证（t3）/ 审查（t4）/ 集成（t5）/ 功能测试（t6）的共同依据。
> 版本：v1（首版）　日期：以 git 提交为准　状态：已与工作区现有代码对齐（复核日期 2026-08-30）

## 1. 批次目标

为 FRT v0.1.15 新增两个功能：

| 编号 | 功能 | 一句话描述 |
|------|------|-----------|
| FR-1 | 启动时自动检查更新 | 程序启动后在后台静默查询 GitHub 最新版；发现新版本时**非侵入提示**（不弹模态框）；网络失败**完全静默**；可配置开关并持久化 |
| FR-2 | 规则模板库 | 内置常见场景规则模板（Minecraft 模组更新 / 资源包 / 配置文件同步等），UI 规则生成向导与控制台向导均可一键套用模板，套用后直接走既有规则文件写入流程 |

**范围限定**：
- FR-1 仅图形界面（`FRTFrame`）生效；控制台模式（`Main.java`）**不新增**自动检查，启动输出保持干净（与前批次「便捷功能批次」的 GUI-only 约定一致）。开关配置统一存于 `config.json`，供 GUI 读写。
- FR-2 影响 `RuleWizardForm`（UI 表单）与 `RuleConfigWizard`（控制台向导）两个入口；不套用模板时，两个入口的行为必须与 v0.1.14 **完全一致**（零回归）。
- 两个功能**不做**自动下载/安装更新、不做模板编辑器的完整 CRUD（模板为内置只读资源，套用后可继续手动修改字段）。
- 不改 `pom.xml`（版本 0.1.7-SNAPSHOT 与 GitHub 地址已就绪，沿用前批次约定；版本号升级/README 更新日志由船长在发布时处理）。
- 零新增第三方依赖（只用已有 Jackson 等）。

## 2. 现状盘点（工作区已有代码，先读后写，避免需求与实现脱节）

### 2.1 FR-1 相关现状

| 文件 | 已有内容 | 本批次关系 |
|------|---------|-----------|
| `service/UpdateChecker.java` | `ReleaseInfo` record、`fetchLatestRelease()`（5s 超时，三层证书兜底：默认信任库 → Windows 系统证书库 → 绕过证书校验，**失败时 `logWarn` 到控制台+文件**）、`isNewer`/`versionSegments` 纯函数 | 自动检查直接复用其查询与版本对比能力；失败日志会出现在 UI 日志区（logback CONSOLE appender → `System.out` → FRTFrame 日志区 tee），与"网络失败静默"冲突，需静默变体（见 FR-1 处理流程） |
| `ui/FRTFrame.java` | 构造末尾加载 config、`runCheckUpdate()`（帮助菜单，SwingWorker 后台查询 + 模态提示）、`handleCheckUpdateResult`（有新版/已最新/失败三分支，均弹 JOptionPane）、`openDownloadPage`、`buildMenuBar(frame)` 静态方法（null frame 可 headless 测菜单结构）、`appendText`/`statusLabel`/`LinkLabel`（状态栏右侧已有版本链接） | 自动检查的接入点：菜单加开关项、构造末尾触发、结果处理复用 `openDownloadPage` 与日志区/状态栏输出 |
| `model/Config.java` | `theme`/`logFontSize`/`maxBackupRecords` 持久化字段先例（含 setter 取值收敛） | 新增 `autoCheckUpdate` 布尔字段（默认 true），遵循同款风格 |
| `core/builder/ConfigLoader.java` | `saveTheme(String)` + 包内可见 `saveThemeTo(Path, String)`：合并写入 config.json 保留其他键、失败仅记日志 | 新增 `saveAutoCheckUpdate(boolean)` + 包内可见 `saveAutoCheckUpdateTo(Path, boolean)`，完全同款模式 |
| `src/test/java/com/awei/frt/ui/FRTFrameMenuTest.java` | `helpMenuHasCheckUpdateAndAbout` 断言帮助菜单第 0 项=检查更新、第 1 项=关于 | 帮助菜单插入"启动时自动检查更新"勾选项后，该测试必须同步更新，否则 t3 全量测试会红 |
| `src/main/resources/logback.xml` | CONSOLE appender 输出 INFO 及以上到 System.out；`com.awei.frt` logger additivity=false 直连 CONSOLE+FILE | 决定了"自动检查失败静默"必须用静默变体获取，或接受日志区出现一条 [警告] 行——本需求要求严格静默 |

### 2.2 FR-2 相关现状

| 文件 | 已有内容 | 本批次关系 |
|------|---------|-----------|
| `service/RuleConfigWizard.java` | 控制台向导：选择作用目录 → 编号文件夹树 → `inputRule()` 逐参数交互 → `writeRuleFile(rule, targetDir)`（预览/确认/写入/自校验，UI 与控制台共用） | 模板询问插在"选择层级之后、`inputRule()` 之前"；套用模板后跳过逐参数输入直接 `writeRuleFile` |
| `ui/RuleWizardForm.java` | 表单式弹窗：作用目录/目标文件夹/主策略参数/策略链；已有 `applyRuleToForm(MatchRule)`（把已解析规则预填到表单，加载已有规则文件时用）；`onOk()` 组装 `MatchRule` 返回 `Result(targetDir, rule)` | 模板套用**复用 `applyRuleToForm`**（模板 rule 深拷贝后预填），不新写字段填充逻辑 |
| `model/MatchRule.java` | `strategyType`/`replacements`/`patterns`/`excludePatterns`/`strategyChain`/`inheritToSubfolders`；`copy()` 深拷贝 | 模板的 `rule` 字段就是 MatchRule 的 JSON 序列化形态 |
| `core/builder/MatchRuleLoader.java` | `fromJson(String)`：解析 + 策略注册表校验（strategyType 与链步骤必须已注册） | 模板加载时逐条用其校验，保证模板规则可被程序正常加载执行 |
| `factory/StrategyFactory.java` | 注册表：`FileSameName` / `McMod` / `ZipEntryName` / `ZipEntryContent`（+ 外部插件动态加载） | 模板 strategyType 必须取自注册表，且对应参数语义正确（见 §4.2） |
| `core/strategy/*.java` | `FileSameName`（replacements：`caseSensitive=false`、`onlyIfContentSame=true`）；`McMod`（目录级，按 modId，replacements：`onlyIfVersionChanged=true`、`onlyIfContentSame=true`，patterns 不生效）；`ZipEntryContent`（zip/jar 内部条目文本含 `contentContains` 关键词即命中，`caseSensitive=false`）；`GlobMatcher.matchesAny` 空 patterns=匹配所有 | 模板内容正确性的依据 |
| `src/main/resources/` | 现含 `config.json`/`logback.xml`/`build-info.properties` | 模板文件存放位置（见 FR-2 存储设计） |
| `testDic/update/matching-rules.json` 等 | 规则文件样例（`FileSameName` 空 patterns 等） | 模板 JSON 形态参考；模板 JSON 必须能被 `MatchRuleLoader.fromJson` 解析 |

### 2.3 与前批次约束的关系（重要）

《需求-便捷功能批次.md》曾明确 **"不做启动时自动检查更新（避免启动依赖网络）"**。本次用户明确要求加回该能力，因此**重新设计**：
- 触发时机：窗口构造完成后**后台线程**执行，不阻塞 EDT、不拖慢启动（可加极短延迟，见 FR-1）；
- 展示方式：发现新版仅状态栏+日志区提示，**不弹模态框**（非侵入）；用户需要详情/下载时点状态栏链接或走既有「帮助 → 检查更新」；
- 失败静默：网络失败/被墙/API 不可达时 **UI 零提示**（不弹窗、状态栏不变、日志区也不显示失败行），仅日志文件保留排查信息；
- 可配置开关：`config.json` 新增 `autoCheckUpdate` 布尔项（默认 **true**），帮助菜单提供勾选项即时切换并持久化。

## 3. 功能需求

### FR-1 启动时自动检查更新

**输入**：无（程序启动即触发，受开关控制）。

**配置模型（`Config`）**：
1. 新增字段 `private boolean autoCheckUpdate = true;`（默认开启——用户明确要求的功能，开箱即用；关掉后手动检查仍可用）；
2. `public boolean isAutoCheckUpdate()` / `public void setAutoCheckUpdate(boolean)`；
3. 读取容错：JSON 缺该键 → 默认 true（Jackson 反序列化时走 Java 默认值）；值非法类型 → 解析失败时配置整体回退默认（沿用既有 loadConfig 失败路径，不特判）。

**持久化（`ConfigLoader`）**：
1. `public static void saveAutoCheckUpdate(boolean)` → 委托 `saveAutoCheckUpdateTo(getExternalConfigPath(), value)`；
2. 包内可见 `static void saveAutoCheckUpdateTo(Path configPath, boolean value)`：**完全照抄 `saveThemeTo` 模式**——读现有文件（去 BOM）→ 合并写 `autoCheckUpdate` 键 → 美化输出写回；文件不存在自动创建；失败仅 `logException` 记日志，不抛异常、不打断 UI。

**菜单开关（`FRTFrame.buildMenuBar`）**：
1. 帮助菜单结构变为：`检查更新`、`启动时自动检查更新`（`JCheckBoxMenuItem`）、分隔线、`关于`；
2. 勾选状态在构建时与 `config.isAutoCheckUpdate()` 同步（frame 为 null 的菜单结构测试只校验结构与文案，勾选状态取当前 config 或默认值均可，但构建真实 frame 时必须同步）；
3. 点击切换：`config.setAutoCheckUpdate(checked)` + `ConfigLoader.saveAutoCheckUpdate(checked)`，即时持久化；日志区记 `[成功] 已开启/关闭启动时自动检查更新`。

**启动触发（`FRTFrame`）**：
1. 构造末尾（`BackupFileLoader.warnOrphanBackupsIfNeeded()` 之后）调用包内可见方法 `void startAutoCheckUpdateIfEnabled()`；
2. 方法逻辑：`config == null` 或 `!config.isAutoCheckUpdate()` → 直接 return（静默）；置检查互斥标志 → `SwingWorker` 后台执行查询 → `done()` 回调调 `handleAutoCheckResult(info)` → 复位标志；
3. 查询复用 `UpdateChecker`（三层证书兜底原样生效）；允许实现加极短延迟（如 1~2s `Timer`）避免与启动争抢带宽，但**不得阻塞 EDT**、不得在窗口显示前弹任何东西；
4. 查询调用建议使用**静默变体**（见下"失败静默"），手动检查路径保持不变。

**结果处理（`handleAutoCheckResult(ReleaseInfo info)`，非侵入）**：
1. `info == null`（网络失败/API 不可达/证书全失败）→ **UI 完全静默**：不弹窗、状态栏不变、日志区不追加任何行；只允许日志**文件**记录排查信息；
2. `info != null` 且 `UpdateChecker.isNewer(info.tagName(), BuildInfo.VERSION)` 为 true → 非侵入提示：
   - 日志区追加一行 `[信息] 发现新版本 vX（当前 vY），可在状态栏点击提示打开下载页`；
   - 状态栏文本置为 `发现新版本 vX`；
   - 状态栏右侧（`versionLink` 旁）显示可点击 `LinkLabel`（文案如 `发现新版本 vX`），点击 → 复用 `openDownloadPage(info)`（`DesktopUtil.openUri`，失败弹一次性提示不崩溃）；
3. `info != null` 且非更新（已最新）→ 静默（不输出任何 UI 内容；可选 `logDebugFileOnly` 记文件）；
4. 状态栏"发现新版本"提示在用户手动执行 `检查更新` 或打开下载页后恢复默认文案（不强制，实现自选，以不残留误导性文案为准）。

**失败静默的关键实现约束**：`UpdateChecker.fetchLatestRelease()` 失败时会 `logWarn("[检查更新] 查询 GitHub 最新版失败: ...")`，logback CONSOLE appender 会把该行打到 `System.out` → FRTFrame 日志区 tee → **用户可见**。因此：
- 允许在 `UpdateChecker` **新增**包内可见静默变体（如 `static ReleaseInfo fetchLatestReleaseQuiet()`，复用同一套三层兜底逻辑，仅失败时不 logWarn 直接返回 null）；
- 自动检查用静默变体，手动「帮助 → 检查更新」继续用带日志版本（行为与 v0.1.14 一致）；
- **不允许**修改既有 `fetchLatestRelease`/`fetchLatestRelease(String)`/`isNewer`/`versionSegments` 的签名与语义（既有测试锁定）。

**互斥（防重复请求）**：
1. `FRTFrame` 增加私有 `volatile boolean updateCheckInProgress`（或等价标志）；
2. `startAutoCheckUpdateIfEnabled()` 与 `runCheckUpdate()`（手动）共用该标志：任一检查进行中，另一触发方直接返回并（手动路径）提示 `[信息] 正在检查更新，请稍候`，不发起第二次网络请求、不出现重复提示框；
3. 标志在 `done()` 回调末尾复位（含异常路径）。

**手动检查不受开关影响**：`帮助 → 检查更新` 无论开关状态均可使用，行为与 v0.1.14 完全一致（模态对话框三分支）。

### FR-2 规则模板库

**模板存储（`src/main/resources/rule-templates.json`）**：
1. 采用**单个聚合 JSON 文件**（classpath 资源），避免 jar 内目录遍历的复杂度；IDE 运行与打包 jar 均可用 `getResourceAsStream` 读取；
2. 顶层结构：
   ```json
   {
     "templates": [
       {
         "id": "mc-mod-update",
         "name": "Minecraft 模组更新（按版本跳过）",
         "category": "Minecraft",
         "description": "按 modId 匹配 jar 执行增/删/改；目标已有同版本模组时跳过替换。规则文件应放在 update 侧的 mods 目录层。",
         "rule": { "strategyType": "McMod", "replacements": { "onlyIfVersionChanged": "true" }, "patterns": [], "excludePatterns": [], "strategyChain": [], "inheritToSubfolders": false }
       }
     ]
   }
   ```
3. `rule` 字段为 `MatchRule` 的标准 JSON 序列化形态（含 `strategyChain` 空数组，与 `writeRuleFile` 输出一致，保证"套用模板 → 表单 → 写回"无字段漂移）；
4. 内置模板清单见 §4.2（7 个，含完整 JSON 草案）；实现可微调 `name`/`description` 文案，但 `id`、`category`、`strategyType`、`replacements`、`patterns`/`excludePatterns` 的**语义必须与清单一致**（t3 逐项核对、t6 实测）。

**模板模型（`model/RuleTemplate.java`，新增）**：
1. 字段：`String id` / `String name` / `String category` / `String description` / `MatchRule rule`，getter/setter（Jackson 直接反序列化）；
2. `public RuleTemplate copy()`：深拷贝（rule 用 `MatchRule.copy()`），供套用时保证模板资源不被表单修改污染。

**模板库加载（`service/RuleTemplateLibrary.java`，新增）**：
1. `public static List<RuleTemplate> loadAll()`：从 classpath 读取 `rule-templates.json`，解析并**逐条校验**——`rule` 必须能被 `MatchRuleLoader.fromJson` 解析（strategyType 与链步骤已注册）；无效模板跳过并 `logWarn`，剩余有效模板照常返回；文件缺失/损坏 → 返回空列表并 `logWarn`，**不抛异常、不崩溃**；
2. `public static RuleTemplate findById(String id)`：按 id 查找，未命中返回 null；
3. 建议静态缓存（模板为内置只读资源，加载一次即可；线程安全无特殊要求——加载发生在 EDT/向导打开时，懒加载单次）；
4. 可测性钩子：提供包内可见 `static List<RuleTemplate> loadFrom(Path jsonPath)`（或等价参数化入口），供测试注入坏文件/临时文件验证容错（参照 `ConfigLoader.saveThemeTo(Path,...)` 的包内钩子先例）。

**UI 表单套用（`ui/RuleWizardForm.java`）**：
1. 表单顶部新增区块"**0. 套用内置模板（可选）**"：`JComboBox`（首项 `（不使用模板）`，其后按加载顺序列出各模板 `name`）+「套用模板」按钮 + 一行灰字提示"选择模板后点套用，可继续修改任意参数"；
2. 点击「套用模板」：从下拉取模板 → `applyRuleToForm(template.getRule().copy())`（**复用既有预填逻辑**：策略类型/patterns/excludePatterns/replacements/继承开关/策略链步骤全部预填）→ 警告区提示"已套用模板「xxx」，可继续修改后生成"（`UITheme.MUTED` 色）；
3. 套用后用户可修改任意字段；「确定生成」走既有 `onOk()` → `RuleConfigWizard.writeRuleFile(rule, targetDir)`（预览/确认/写入/自校验）——这就是"规则文件写入一键套用模板"；
4. 模板加载失败（空列表）→ 下拉只有 `（不使用模板）`，提示"模板库加载失败，可手动填写"，不影响手动流程；
5. 与既有行为的关系：套用模板后若用户切换目标文件夹，`reloadFromSelectedFolder()` 仍按既有逻辑预填已有规则文件（覆盖模板预填是**预期行为**，不特殊处理）；
6. 不选模板时，表单外观/行为与 v0.1.14 完全一致（模板区块仅多一个可选行）。

**控制台向导套用（`service/RuleConfigWizard.java`）**：
1. 在"选择层级"完成之后、`inputRule(targetDir)` 之前插入询问：`[选择] 是否套用内置规则模板？(y/n, 回车=n)`；
2. `y` → 列出模板：`编号=名称[分类]（描述）`（与 `listStrategies` 同风格）→ `请输入模板编号 (0/回车=取消)` → 合法编号 → `MatchRule rule = template.getRule().copy()` → **跳过逐参数输入**，直接进入 `writeRuleFile(rule, targetDir)`（预览/确认/写入/自校验）；
3. 无效编号 → 提示 `[失败] 无效模板编号` 并重新询问（或返回继续手动流程，实现自选，以不产生死循环为准）；
4. 模板加载失败 → 提示"模板库加载失败，继续手动输入"并走既有逐参数流程；
5. `n`/回车 → 走既有流程，**输出与 v0.1.14 逐字一致**（零回归）。

## 4. 交互与文案约定

- 日志区沿用既有 `[成功]` 绿 / `[失败]` 红 / `[警告]` 橙 / `[信息]` 青样式；自动检查的新版提示用 `[信息]`；
- 自动检查**任何路径不弹 JOptionPane**；只有用户主动点击（状态栏链接/帮助菜单）才弹；
- 所有文案中文，语气与既有 UI 一致；
- 模板名称/描述用中文，直接展示在 UI 下拉与控制台列表。

### 4.2 内置模板清单（需求锁定内容，JSON 草案）

> 说明：`rule` 中 `patterns: []` 表示"匹配所有"（`GlobMatcher.matchesAny` 语义）；`strategyChain: []` 与 `writeRuleFile` 序列化输出一致。策略参数语义以 §2.2 现状为准。

**T1 `mc-mod-update`** — Minecraft 模组更新（按版本跳过）
- category: `Minecraft`
- description: 按 modId 匹配 jar 执行增/删/改；目标已有同版本模组时跳过替换。规则文件放在 update 侧的 mods 目录层。
- rule:
```json
{
  "strategyType": "McMod",
  "replacements": { "onlyIfVersionChanged": "true" },
  "patterns": [],
  "excludePatterns": [],
  "strategyChain": [],
  "inheritToSubfolders": false
}
```

**T2 `mc-mod-update-content`** — Minecraft 模组更新（内容比对，更准确）
- category: `Minecraft`
- description: 按 modId 匹配 jar 执行增/删/改；源与目标文件 MD5 相同（含同版本重打包）则跳过替换。适用于需要精确同步的场景。
- rule:
```json
{
  "strategyType": "McMod",
  "replacements": { "onlyIfContentSame": "true" },
  "patterns": [],
  "excludePatterns": [],
  "strategyChain": [],
  "inheritToSubfolders": false
}
```

**T3 `mc-mod-plus-files`** — Minecraft 模组目录组合更新（模组 + 目录内其他文件）
- category: `Minecraft`
- description: 策略链示例：第 1 步 McMod 按 modId 处理模组 jar；剩余文件（非 mod jar、配置等）由第 2 步 FileSameName 按文件名同步（排除临时文件）。规则文件放在 update 侧的 mods 目录层。
- rule:
```json
{
  "strategyType": "McMod",
  "replacements": { "onlyIfVersionChanged": "true" },
  "patterns": [],
  "excludePatterns": [],
  "strategyChain": [
    {
      "strategyType": "FileSameName",
      "replacements": {},
      "patterns": [],
      "excludePatterns": ["*.bak", "*.tmp", "*~"]
    }
  ],
  "inheritToSubfolders": false
}
```

**T4 `mc-resourcepack-sync`** — Minecraft 资源包同步
- category: `Minecraft`
- description: 只更新压缩包内部含 `pack_format` 文本（即含 pack.mcmeta 的资源包）的 zip/jar 资源包；其余压缩包跳过。
- rule:
```json
{
  "strategyType": "ZipEntryContent",
  "replacements": { "contentContains": "pack_format" },
  "patterns": [],
  "excludePatterns": [],
  "strategyChain": [],
  "inheritToSubfolders": false
}
```

**T5 `config-sync`** — 配置文件同步
- category: `配置同步`
- description: 按文件名同步常见配置文件（properties/xml/json/yml/yaml/toml/ini/cfg），排除备份与临时文件。规则文件放在 update 侧对应配置目录层。
- rule:
```json
{
  "strategyType": "FileSameName",
  "replacements": {},
  "patterns": ["*.properties", "*.xml", "*.json", "*.yml", "*.yaml", "*.toml", "*.ini", "*.cfg"],
  "excludePatterns": ["*.bak", "*.tmp", "*.old", "*~"],
  "strategyChain": [],
  "inheritToSubfolders": false
}
```

**T6 `file-sync-all`** — 通用文件全量同步
- category: `通用`
- description: 按文件名匹配同步全部文件（空白名单=匹配所有），排除备份与临时文件。适用于任意目录的整目录同步。
- rule:
```json
{
  "strategyType": "FileSameName",
  "replacements": {},
  "patterns": [],
  "excludePatterns": ["*.bak", "*.tmp", "*~"],
  "strategyChain": [],
  "inheritToSubfolders": false
}
```

**T7 `file-sync-content`** — 通用内容比对同步（跳过相同文件）
- category: `通用`
- description: 按文件名同步；源与目标 MD5 相同则跳过替换（只更新真正变化的文件）。
- rule:
```json
{
  "strategyType": "FileSameName",
  "replacements": { "onlyIfContentSame": "true" },
  "patterns": [],
  "excludePatterns": ["*.bak", "*.tmp", "*~"],
  "strategyChain": [],
  "inheritToSubfolders": false
}
```

## 5. 非功能需求（NFR）

1. **不阻塞 EDT**：自动检查的网络查询必须在后台线程（SwingWorker）；模板加载在向导打开时同步执行（本地资源，毫秒级，可接受），但**不得**在启动路径加载模板（模板只在打开向导时加载）；
2. **全链路静默降级**：自动检查网络失败 UI 零打扰；模板缺失/损坏回退为无模板，不影响手动流程；任何分支不抛未捕获异常；
3. **零新增依赖**：只用已有 Jackson/SLF4J/Logback/Swing；
4. **Java 17 兼容**：编译目标 17，无平台特有 API（证书兜底已覆盖 Win/macOS/Linux）；
5. **既有功能零回归**：手动「检查更新」行为不变；不选模板时两个向导行为与 v0.1.14 一致；主题/字体/备份恢复/清理等不受影响；
6. **可测试性**：配置默认值/持久化、菜单结构、模板加载校验、模板 JSON 可解析性、自动检查结果决策逻辑必须 headless 可测；真实网络、弹窗、状态栏链接点击、模板表单交互列为静态核对 + 用户实测（t6）。

## 6. 实现约束（t2 必须遵守）

1. **不改** `UpdateChecker` 既有方法的签名与语义（`fetchLatestRelease` 两个版本、`latestReleaseApiUrl` 两个版本、`isNewer`、`versionSegments`；测试锁定）。允许**新增**包内可见静默变体（如 `fetchLatestReleaseQuiet()`）供自动检查使用；
2. **不改** `Config` 既有字段/getter/setter 语义；新增 `autoCheckUpdate` 字段遵循 `logFontSize`/`theme` 先例（含取值收敛风格）；
3. `ConfigLoader.saveAutoCheckUpdate` + 包内可见 `saveAutoCheckUpdateTo(Path, boolean)` **照抄 `saveThemeTo` 模式**（合并写入、保留其他键、去 BOM、失败仅记日志）；
4. 自动检查结果处理**非侵入**：启动路径绝不弹 JOptionPane；失败路径不写 UI 日志区、不改状态栏；
5. 菜单项/启动触发与既有风格一致：SwingWorker 后台、`appendText`/`statusLabel`/`LinkLabel`、`UITheme` 主题色；状态栏链接复用 `LinkLabel`（与 `versionLink` 同款，见其 HTML 锚点注释，不调无效 setForeground）；
6. 模板 `rule` 的 `strategyType` 必须来自 `StrategyFactory` 注册表；模板 JSON 必须是 `MatchRule` 可序列化/反序列化的标准形态；模板套用必须复用 `applyRuleToForm`（UI）与 `writeRuleFile`（两入口共用）；
7. `RuleWizardForm` 模板区块放在表单顶部（"0. 套用内置模板"），不重排既有区块顺序；控制台模板询问插在层级选择后、`inputRule` 前；
8. **更新** `FRTFrameMenuTest.helpMenuHasCheckUpdateAndAbout` 以匹配新帮助菜单结构（检查更新 / 启动时自动检查更新 / 关于），并新增勾选状态断言（buildMenuBar(null) 时状态取默认或当前 config，须与实现一致）；
9. 不触碰 `.agent-teams/`、`.dsh-*` 等运行时目录；不改 `pom.xml`；控制台模式 `Main.java` 无 diff（模板询问在 `RuleConfigWizard` 内部，不动 Main）；
10. 新增/改动文件中文注释、命名与既有风格一致；纯逻辑测试放对应包（参照 `LogFontSizeSaveTest`、`FRTFrameMenuTest` 模式），文件写入类测试用 `@TempDir` 隔离。

## 7. 验收标准（t3 逐条执行；t4 审查引用；t6 实测引用）

### AC-1 编译与打包
- [ ] AC-1.1 `mvn -o compile` 通过（main 全量）；
- [ ] AC-1.2 `mvn -o test` 全绿（既有全部测试 + 本批次新增测试 + 更新后的 FRTFrameMenuTest）；
- [ ] AC-1.3 `mvn -o package -DskipTests` 成功产出可执行 jar。

### AC-2 开关配置与持久化（纯逻辑）
- [ ] AC-2.1 `new Config().isAutoCheckUpdate()` 默认 **true**；
- [ ] AC-2.2 `setAutoCheckUpdate(false)` → `isAutoCheckUpdate()==false`；true → true；
- [ ] AC-2.3 `saveAutoCheckUpdateTo(tempDir/config.json, false)`：合并写入——`autoCheckUpdate=false` 且其他键（logLevel/updatePath/theme 等）保留；
- [ ] AC-2.4 `saveAutoCheckUpdateTo` 目标文件不存在时自动创建；
- [ ] AC-2.5 保存失败（只读路径/IO 异常）不抛异常、仅记日志（测试或静态核对）。

### AC-3 启动自动检查更新行为（静态核对 + 决策逻辑单测）
- [ ] AC-3.1 `startAutoCheckUpdateIfEnabled()`：`config==null` 或开关关闭 → 直接返回，不发起查询；
- [ ] AC-3.2 查询在 SwingWorker 后台线程执行，不在 EDT 上网络等待；
- [ ] AC-3.3 自动检查使用静默变体（失败不产生用户可见日志/弹窗）；
- [ ] AC-3.4 发现新版：日志区一行 `[信息]` + 状态栏 `发现新版本 vX` + 状态栏可点击链接（→ `openDownloadPage`），**无模态弹窗**；
- [ ] AC-3.5 已最新：UI 无任何输出（静默）；
- [ ] AC-3.6 失败（info==null）：UI 完全静默（不弹窗、状态栏不变、日志区无失败行）、不崩溃；
- [ ] AC-3.7 手动「检查更新」不受开关影响，且与自动检查互斥（进行中再触发 → 提示"正在检查更新，请稍候"，不重复请求/重复弹窗）；
- [ ] AC-3.8 开关关闭时启动：无任何自动检查动作（可静态核对首行判断）。

### AC-4 菜单与开关 UI
- [ ] AC-4.1 帮助菜单结构：`检查更新` / `启动时自动检查更新`（JCheckBoxMenuItem）/ 分隔线 / `关于`（文案与顺序一致）；
- [ ] AC-4.2 构建真实 frame 时勾选状态与 `config.isAutoCheckUpdate()` 一致；
- [ ] AC-4.3 切换勾选：`config.setAutoCheckUpdate` + `saveAutoCheckUpdate` 即时持久化，config.json 合并保留其他键；
- [ ] AC-4.4 `FRTFrameMenuTest` 已更新且全绿（帮助菜单新结构 + 勾选状态断言）。

### AC-5 规则模板库（资源与加载，纯逻辑）
- [ ] AC-5.1 `src/main/resources/rule-templates.json` 存在且 JSON 合法；
- [ ] AC-5.2 模板清单与 §4.2 一致（7 个：id/name/category/description 齐全，id 唯一）；
- [ ] AC-5.3 `RuleTemplateLibrary.loadAll()` 返回全部有效模板；**每条模板的 rule 经 `MatchRuleLoader.fromJson` 解析非 null**（strategyType 已注册、链步骤已注册）；
- [ ] AC-5.4 模板文件缺失/损坏/含无效项：loadAll() 返回空列表或跳过无效项，不抛异常（包内测试钩子注入坏文件验证）。

### AC-6 模板套用（UI 表单）
- [ ] AC-6.1 `RuleWizardForm` 顶部含模板区块：下拉（首项 `（不使用模板）`）+「套用模板」按钮 + 提示行；
- [ ] AC-6.2 套用后表单预填与模板 rule 一致（策略类型/patterns/excludePatterns/replacements/继承开关/策略链步骤数量与内容）；
- [ ] AC-6.3 套用后仍可修改字段，「确定生成」走 `writeRuleFile`，生成文件 JSON 与表单最终值一致（含预览/确认/自校验）；
- [ ] AC-6.4 模板加载失败 → 下拉仅 `（不使用模板）` + 提示，手动填写流程不受影响；
- [ ] AC-6.5 不套用模板时表单行为与 v0.1.14 一致（既有测试/静态核对）。

### AC-7 模板套用（控制台向导）
- [ ] AC-7.1 选择层级后询问是否套用模板；y → 编号=名称[分类]（描述）列表；
- [ ] AC-7.2 套用后跳过逐参数输入，直接进入 `writeRuleFile`（预览/确认/写入/自校验），写入内容与模板 rule 一致；
- [ ] AC-7.3 模板加载失败 → 提示并回退手动逐参数流程，不崩溃；
- [ ] AC-7.4 不套用（n/回车）时输出与 v0.1.14 一致（零回归）。

### AC-8 模板内容正确性（逐项核对）
- [ ] AC-8.1 T1 mc-mod-update：McMod + `onlyIfVersionChanged=true`；
- [ ] AC-8.2 T2 mc-mod-update-content：McMod + `onlyIfContentSame=true`；
- [ ] AC-8.3 T3 mc-mod-plus-files：主策略 McMod + 链步骤 FileSameName（顺序/参数正确）；
- [ ] AC-8.4 T4 mc-resourcepack-sync：ZipEntryContent + `contentContains=pack_format`；
- [ ] AC-8.5 T5 config-sync：FileSameName + 配置文件扩展名 patterns + 排除项；
- [ ] AC-8.6 T6 file-sync-all / T7 file-sync-content：FileSameName 语义正确（空白名单=全量 / onlyIfContentSame=true）。

### AC-9 静态核对（验证工程师/审查官）
- [ ] AC-9.1 无新增第三方依赖（pom.xml 未改）；
- [ ] AC-9.2 无 EDT 上网络等待；无 System.exit；自动检查失败路径无 UI 弹窗/日志区输出；
- [ ] AC-9.3 `Main.java` 无 diff（或仅注释级）；控制台菜单不回归；
- [ ] AC-9.4 新增/改动文件中文注释、风格一致；无裸 `printStackTrace`/新增裸 `System.out.println`（模板加载与自动检查路径日志全部走 LoggerUtil）。

## 8. 测试需求（t2 实现，t3 执行）

1. **开关配置测试**（新增，参照 `LogFontSizeSaveTest` 模式放 `core/builder` 包）：AC-2 全部（`@TempDir` 隔离 config.json 写入污染）；
2. **菜单结构测试**（更新 `FRTFrameMenuTest`）：帮助菜单新结构（检查更新 / 启动时自动检查更新 / 关于）与勾选状态（buildMenuBar(null) 时状态取实现约定值，注释说明）；
3. **自动检查决策逻辑测试**（新增，若实现把结果处理抽成可测方法——推荐包内可见/静态决策方法，如 `static AutoCheckOutcome decideAutoCheck(ReleaseInfo info, String currentVersion)` 或等价）：null→静默、新版→提示、同版/更旧→静默，三分支无异常；互斥标志复位（若可测）；
4. **模板库测试**（新增，`service` 包）：AC-5.3/5.4（读取 classpath 真实模板验证全部可解析；经包内 `loadFrom(Path)` 钩子注入坏文件/无效项验证容错）；
5. **模板 JSON 与 MatchRule 一致性**：模板 rule 经 `MatchRuleLoader.fromJson` 非 null 且字段与清单一致（可并入模板库测试，逐项断言 T1~T7 关键字段）；
6. **既有测试零回归**：`mvn -o test` 全绿（现有 123 个测试 + 本批次新增/更新）。
7. GUI 交互项（AC-3.4 状态栏链接、AC-6 表单套用、AC-7 控制台套用）headless 无法自动化的部分，由 t3 静态核对 + t6 用户实测覆盖。

## 9. 风险与回归关注点（t4 审查重点）

1. **自动检查与手动检查互斥**：两路径共用标志；任何一方 `done()` 异常也要复位标志，否则后续手动检查永远被"正在检查"挡住；
2. **"静默"被 logback 破坏**：`fetchLatestRelease` 的失败 logWarn 会经 CONSOLE appender 进 UI 日志区——自动检查必须走静默变体；审查逐行确认自动检查失败路径无 `LoggerUtil.logWarn/logError`（普通 logger）、无 `appendText`；
3. **非侵入 vs 模态**：自动检查路径（含状态栏链接回调）不得引入 JOptionPane 模态弹窗；手动检查路径行为不得被改动；
4. **开关持久化并发**：`saveAutoCheckUpdate` 与 `saveTheme`/`saveLogFontSize` 同为"读-改-写"整文件；本批次不新增并发入口（与既有行为一致），审查确认无新增并发写点；
5. **启动顺序**：`startAutoCheckUpdateIfEnabled()` 必须在 config 加载成功之后调用；`config==null`（配置加载失败）时自动检查静默跳过（与既有 `[失败] 配置加载失败` 提示不冲突）；
6. **菜单测试连锁**：帮助菜单插入勾选项会改 `FRTFrameMenuTest` 断言——若漏改，t3 全量测试红；实现与测试必须同批提交；
7. **模板字段漂移**：模板 rule 若与 `MatchRule` 序列化形态不一致（如缺 `strategyChain` 键、多余未知键），套用→表单→写回可能出现字段丢失/冗余；要求模板 rule 与 `writeRuleFile` 输出同构（`strategyChain: []` 显式保留），审查核对；
8. **模板套用覆盖用户输入**：套用按钮必须显式触发（不随下拉选择即覆盖）；套用后 warningLabel 提示可继续修改，避免用户误以为已锁定；
9. **控制台交互回归**：模板询问插入位置不得改变不选模板时的既有输出顺序与文案；无效输入不得死循环；
10. **模板加载异常**：`rule-templates.json` 在打包 jar 中必须存在（Maven resources 默认包含 `src/main/resources`）；加载失败路径必须静默回退，不得在向导打开时抛异常。

## 10. 交付物（t5 集成范围）

- 本需求文档（`需求-v0.1.15-启动自动检查更新与规则模板库.md`）；
- 代码（FR-1）：`Config` 加 `autoCheckUpdate`；`ConfigLoader` 加 `saveAutoCheckUpdate`/`saveAutoCheckUpdateTo`；`UpdateChecker` 新增静默变体（若实现选择该方案）；`FRTFrame` 帮助菜单开关项 + `startAutoCheckUpdateIfEnabled()` + `handleAutoCheckResult()` + 状态栏链接 + 互斥标志；
- 代码（FR-2）：`model/RuleTemplate.java`、`service/RuleTemplateLibrary.java`、`src/main/resources/rule-templates.json`（7 个模板）、`RuleWizardForm` 模板区块、`RuleConfigWizard` 模板询问；
- 测试：开关配置测试、`FRTFrameMenuTest` 更新、自动检查决策测试（若可测）、`RuleTemplateLibraryTest`；
- 提交信息建议：`feat(v0.1.15): 启动自动检查更新 + 规则模板库（含 Minecraft 模板）`（最终由船长定夺；本地提交 dev，不推送；tag/发布由船长处理）。
