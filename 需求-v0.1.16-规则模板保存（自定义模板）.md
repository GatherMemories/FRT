# FRT v0.1.16 功能批次——需求说明与验收标准

> 需求分析师产出，作为实现（t2）/ 验证（t3）/ 功能测试（t4）/ 审查（t5）/ 集成（t6）的共同依据。
> 版本：v1（首版）　日期：以 git 提交为准　状态：已与工作区现有代码对齐（复核日期 2026-08-30）

## 1. 批次目标

为 FRT v0.1.16 新增一个功能：

| 编号 | 功能 | 一句话描述 |
|------|------|-----------|
| FR-1 | 规则模板保存（自定义模板） | 用户在规则生成（GUI 表单 + 控制台向导）中可把**当前规则配置**保存为自定义模板；自定义模板与内置模板（`rule-templates.json` 7 个）**合并展示**、一键复用，并支持删除/改名（最小集管理） |

**范围限定**：
- FR-1 影响 `RuleWizardForm`（GUI 表单）与 `RuleConfigWizard`（控制台向导）两个规则生成入口；内置模板（classpath 只读资源，7 个）**内容不变、只读保护、不可删除/改名**，v0.1.15 的既有加载/套用行为零回归。
- 自定义模板持久化为**独立用户文件**（格式与内置 `rule-templates.json` 同构，`RuleTemplate` 模型复用），与内置模板**天然区分**（来源不同：classpath 内置 / 用户文件），不向 JSON 新增"自定义"标记字段。
- 管理为**最小集**：保存（含重名覆盖询问）、删除、改名；GUI 提供全部三项，控制台仅提供保存（删除/改名由 GUI 管理入口覆盖，见 §3.6）。
- **不做**模板导入/导出、**不做**在线模板拉取/同步、**不做**模板分类管理 UI（category 仅文本展示）、**不做**内置模板内容修改、**不改** `pom.xml`（版本号升级/README 更新日志由船长在发布时处理）、零新增第三方依赖。
- 不套用/不保存时，两个规则生成入口的行为必须与 v0.1.15 **完全一致**（零回归；控制台输出逐字一致）。

## 2. 现状盘点（工作区已有代码，先读后写，避免需求与实现脱节）

### 2.1 FR-1 相关现状

| 文件 | 已有内容 | 本批次关系 |
|------|---------|-----------|
| `model/RuleTemplate.java` | `id/name/category/description/rule` 五字段 + `copy()` 深拷贝（rule 用 `MatchRule.copy()`）；`@JsonIgnoreProperties(ignoreUnknown=true)` | **直接复用**作为自定义模板模型；保存/加载无需新模型 |
| `service/RuleTemplateLibrary.java` | 静态懒加载 classpath `rule-templates.json`；`loadAll()`/`findById()`；`parse(byte[])` 逐条校验（缺 id/rule 跳过 + `MatchRuleLoader.fromJson` 校验）；BOM 兼容；包内测试钩子 `loadFrom(Path)`；失败一律静默回退空列表 | 扩展点：新增自定义模板的**存储路径解析**、**loadAllCustom()/loadAllMerged()**、**save/delete/rename**；`parse` 校验逻辑复用（抽公共方法或复制同款逻辑） |
| `core/builder/MatchRuleLoader.java` | `fromJson(String)`：解析 + 策略注册表校验（strategyType 与链步骤必须已注册），不合法返回 null | **保存前校验的唯一依据**：把组装好的规则序列化为 JSON → `fromJson != null` 才允许保存 |
| `ui/RuleWizardForm.java` | 表单式弹窗；顶部模板区块（`templateCombo` 首项 `（不使用模板）` + 「套用模板」按钮）；`applyRuleToForm(MatchRule)` 预填；`onOk()` 组装 `MatchRule`（策略类型/patterns/excludePatterns/replacements/继承开关/策略链）返回 `Result(targetDir, rule)` | 新增「保存为模板」按钮 + 「管理自定义模板」入口；`onOk()` 的规则组装逻辑**抽取为可复用方法**（`buildRuleFromForm()`），保存按钮复用；模板下拉改为合并列表（内置 + 自定义，自定义带标记） |
| `service/RuleConfigWizard.java` | 控制台向导：`start()` → 选目录 → 编号文件树 → `askTemplateOrInput(targetDir)`（FR-2 模板询问）→ `writeRuleFile(rule, targetDir)`（预览/确认/写入/自校验，UI 与控制台共用） | 新增保存询问 `maybeSaveAsTemplate(rule)`，插在 `askTemplateOrInput` 返回后、`writeRuleFile` 预览前（手动输入与套用模板两条路径都覆盖） |
| `src/main/resources/rule-templates.json` | 内置 7 模板聚合文件，顶层 `{"templates":[...]}`，rule 为 MatchRule 标准形态（含 `strategyChain: []`） | 自定义模板文件**同构**；内置文件本批次**零改动** |
| `core/builder/ConfigLoader.java` | 外部 config.json 路径模式：`Paths.get(".").normalize().toAbsolutePath().resolve("config.json")`；`saveThemeTo(Path,...)` 包内测试钩子先例（读现有→去 BOM→合并→美化写回，失败仅记日志） | 自定义模板存储位置参照其**工作目录**定位；文件写模式参照 `saveThemeTo`（整文件读改写、UTF-8、BOM 兼容、失败不抛异常） |
| `start-frt.sh` / `start-frt.bat` | 启动脚本先 `cd` 到脚本所在目录（= 发布包解压目录）再启动 jar | 发布场景下**工作目录即用户解压目录（用户可写）**，`<工作目录>/templates/` 满足"发布包只读场景可用"（jar 内置资源只读，自定义模板存包外） |
| `src/test/java/com/awei/frt/service/RuleTemplateLibraryTest.java` | 锁定内置 7 模板、`findById`/`copy()` 深拷贝、`loadFrom` 容错 | 既有断言**必须保持全绿**（findById 语义扩展后仍兼容）；新增自定义模板读写/校验/删除/改名测试 |
| `src/test/java/com/awei/frt/RuleConfigWizardTest.java` | 3 个端到端测试用脚本化 `UserPrompter` 驱动完整流程（输入含 `"n"`=不套用模板等） | 新增保存询问后，**既有 3 个测试脚本需各追加一个"n"/回车**（不保存）——实现与测试必须同批更新，否则 t3 全量测试红 |
| `ui/UITheme.java` | 工作区含**未提交的弹窗深色主题修复**（`Panel.foreground`/`Label.foreground`/`TextField` 等 UIManager 键随主题，38 行新增） | 新增「保存为模板」输入对话框/「管理自定义模板」对话框**必须用 UITheme 样式组件**（深色主题下可读）；该修复由 t6 集成时与船长协调一并提交 |

### 2.2 与 v0.1.15 的关系与约束

- v0.1.15 引入 `RuleTemplate`/`RuleTemplateLibrary`（内置只读模板库）与两向导的模板套用能力。v0.1.16 在其架构上**新增"用户可写"来源**，复用校验/套用/写回全链路。
- **不得改动** v0.1.15 已锁定的行为：`loadAll()` 仍返回内置 7 模板（既有测试断言数量）；`RuleTemplate` 字段/`copy()` 语义不变；`rule-templates.json` 内容不变；不套用模板时两向导输出不变。
- 控制台保存询问会**新增一个交互步骤**（回车默认 n），这是 v0.1.16 的有意变更，需同步更新 `RuleConfigWizardTest` 既有脚本（见 §8 测试需求 3）。

## 3. 功能需求

### 3.1 存储位置（用户可写目录，发布包只读场景可用）

**用户模板文件**：单个聚合 JSON 文件，路径解析顺序（两级，主位置失败回退）：

1. **主位置**：`<工作目录>/templates/user-templates.json`
   - 工作目录 = `Paths.get(".").normalize().toAbsolutePath()`（与 `ConfigLoader.getExternalConfigPath()` 同一目录，即外部 `config.json` 所在目录）；
   - 发布包场景：启动脚本先 `cd` 到解压目录，用户解压的目录可写 → 主位置可用；jar 内置资源（含 `rule-templates.json`）保持只读，自定义模板写包外；
   - `templates/` 目录不存在时自动创建（`Files.createDirectories`，失败走回退）。
2. **兜底位置**：`<user.home>/.frt/templates/user-templates.json`
   - 仅当主位置 `templates/` 目录创建或文件写入失败（如安装在只读目录）时使用。
3. 两级均失败 → 保存/删除/改名返回失败状态，**提示用户但不抛异常、不打断向导流程**；加载时两级均不可读 → 空自定义模板集。

**路径解析实现要求**：
- 集中为一个方法（如 `getCustomTemplatesFile()`）负责两级定位，包内可见参数化钩子（如 `resolveCustomTemplatesFile(Path workingDir, Path homeDir)` 或静态覆盖字段，参照 `ConfigLoader.setBackupPathForTesting` 先例）供测试注入临时路径，**测试必须用 `@TempDir` 隔离，不得写真实工作目录**；
- 文件不存在视为"无自定义模板"（空列表），加载不报错；`templates/` 目录缺失时加载也按空处理（只读探测，不强制创建——只有保存时才创建目录）。

### 3.2 持久化格式（与内置模板同构）

`user-templates.json` 顶层结构与 `rule-templates.json` 完全一致：

```json
{
  "templates": [
    {
      "id": "custom-1750000000000",
      "name": "我的模组同步方案",
      "category": "自定义",
      "description": "……",
      "rule": {
        "strategyType": "McMod",
        "replacements": { "onlyIfVersionChanged": "true" },
        "patterns": [],
        "excludePatterns": [],
        "strategyChain": [],
        "inheritToSubfolders": false
      }
    }
  ]
}
```

- **模型复用** `RuleTemplate`（Jackson 直接反序列化/序列化），不新增字段、不新增"自定义"标记字段——区分来自**来源**（classpath 内置 / 用户文件），展示标记由 UI/控制台层生成（见 §3.4）；
- `rule` 必须与 `writeRuleFile` 输出同构（`strategyChain: []` 显式保留，`MatchRule` 默认序列化即含空数组，保证"保存 → 套用 → 写回"无字段漂移）；
- 写入用 UTF-8 + 美化输出（`writerWithDefaultPrettyPrinter`）；读取去 BOM（复用 `parse` 的 BOM 处理）；文件损坏/含无效项 → 跳过无效项或空列表 + `logWarn`，不抛异常（与内置加载容错一致）；
- 自定义模板 `id` 保存时自动生成：`custom-<时间戳/短随机>`（实现自选），保证不与内置 id、不与现有自定义 id 冲突；`name` 必填（非空非空白），`category` 默认 `"自定义"`（可填），`description` 可选。

### 3.3 模板库扩展（`service/RuleTemplateLibrary.java`）

在**不改动既有方法语义**的前提下新增（推荐 API 形态，实现可微调签名，行为必须满足）：

| 方法 | 行为 |
|------|------|
| `getCustomTemplatesFile()` | 返回解析后的用户模板文件路径（§3.1 两级定位）；包内参数化钩子供测试注入 |
| `loadAllCustom()` | 读取并校验用户文件（复用/共享 `parse` 校验逻辑）；文件缺失/损坏 → 空列表 + `logWarn`，不抛异常；**每次调用重读**（文件极小，避免缓存失效问题），静态缓存仅限内置 |
| `loadAllMerged()` | 内置 + 自定义合并列表（**内置在前、自定义在后**）；不可修改视图 |
| `findById(String)` | 语义扩展：内置优先，未命中再查自定义（既有"内置 7 模板"断言与 `no-such-template` 断言均不受影响）；仍不可变列表 |
| `saveTemplate(RuleTemplate, boolean overwrite)` | ①校验：id/name 非空、rule 非空，且 **rule 序列化为 JSON 后经 `MatchRuleLoader.fromJson` 非 null**（保存前校验，§3.4）；②加载现有自定义列表：同 id → 更新字段；同 name 不同 id 且 `overwrite=false` → 返回 `DUPLICATE_NAME`（由调用方询问后带 `overwrite=true` 重调）；同 name 且该 name 是**内置模板名** → 拒绝（内置只读，不可覆盖）；③整文件读改写（去 BOM、合并、美化写回）；④返回状态：`SUCCESS / DUPLICATE_NAME / INVALID_RULE / IO_ERROR`（或等价枚举/布尔组合，**保存失败绝不抛异常到调用方**） |
| `deleteTemplate(String id)` | 仅作用于自定义集合：id 存在 → 删除该条并整文件写回 → true；**id 为内置模板 → 拒绝返回 false**（内置只读保护）；id 不存在 → 返回 false（不抛异常） |
| `renameTemplate(String id, String newName)` | 仅自定义集合：改名不换 id；newName 非空且不与内置名/其他自定义名冲突 → 更新 name 写回 → true；否则 false |
| 包内钩子 | `loadCustomFrom(Path)` / `saveCustomTo(Path, ...)` / `deleteCustomFrom(Path, id)` / `renameCustomIn(Path, ...)`（参照 `ConfigLoader.saveThemeTo(Path,...)` 先例），测试注入临时路径隔离污染 |

### 3.4 合并展示与区分

- **数据层**：`loadAllMerged()` = 内置（前）+ 自定义（后）；内置/自定义**天然区分**，JSON 无标记字段；
- **GUI 表单**（`RuleWizardForm` 模板区块）：下拉顺序为 `（不使用模板）` → 内置模板（原名）→（可选分隔项）→ 自定义模板（显示名带标记，如 `【自定义】名称` 或 `名称（自定义）`）；套用逻辑改用**显示名 → RuleTemplate 映射**（避免索引错位；既有 `templates` 列表与下拉一一对应的实现需相应调整）；「套用模板」对内置与自定义一视同仁（`applyRuleToForm(t.getRule().copy())`）；
- **控制台向导**（`RuleConfigWizard.askTemplateOrInput`）：编号列表 = 内置 `1..N` + 自定义 `N+1..`，自定义行尾追加 `[自定义]` 标记（如 `3=我的方案[自定义]（描述）`）；编号连续，套用逻辑不变；
- 内置模板的显示文案（name/category/description）保持 v0.1.15 原样。

### 3.5 GUI 表单：保存与管理

**「保存为模板」按钮**（`RuleWizardForm` 底部按钮行，推荐放在「确定生成」左侧，不重排既有区块）：

1. 点击 → 用抽取出的 `buildRuleFromForm()` 组装当前表单规则（策略类型/patterns/excludePatterns/replacements/继承开关/策略链；**不含**作用目录/目标文件夹——模板只存规则，与内置模板语义一致）；
2. **保存前校验**：规则序列化 → `MatchRuleLoader.fromJson` 非 null → 通过；失败 → 弹窗提示"当前规则不合法，无法保存为模板"（附原因），不写文件、不关闭表单；
3. 通过 → 弹输入对话框收集：模板名称（**必填**，非空）、分类（可选，默认"自定义"）、描述（可选）——可用 `JOptionPane.showInputDialog` 或小型样式化对话框（**必须用 UITheme 样式，深色主题可读**）；
4. 名称与内置模板重名 → 拒绝并提示换名；与自定义模板重名 → 询问"已存在同名模板，是否覆盖？"→ 覆盖 = 更新该名称模板全部字段（保持原 id）；
5. 保存成功 → **刷新模板下拉**（含新增自定义项，带标记），提示"已保存自定义模板「名称」"（`UITheme.SUCCESS`/`MUTED` 色），**不关闭表单**，用户可继续「确定生成」；
6. 保存失败（IO）→ 提示失败原因，不崩溃、不打断表单。

**「管理自定义模板」入口**（模板区块内，与「套用模板」同排或紧邻；实现为小对话框 `ui/TemplateManageDialog.java` 或表单内嵌，自选）：

1. 对话框**仅列出自定义模板**（名称/分类/描述）——内置模板不展示，删除入口对内置**隐藏**（从源头杜绝误删；同时满足"内置只读保护"）；
2. 每行「删除」：确认（JOptionPane 确认框）→ `deleteTemplate(id)` → 刷新列表与表单下拉；内置 id 不可达（不展示），库层 `deleteTemplate` 对内置 id 仍返回 false（双保险）；
3. 每行「改名」：弹窗输入新名称（非空、不与内置/其他自定义重名）→ `renameTemplate(id, newName)` → 刷新；
4. 对话框与表单模板下拉**共享同一刷新路径**（保存/删除/改名后下拉立即反映最新状态）；
5. 无自定义模板时：管理入口可置灰或打开后显示"暂无自定义模板"（自选，以不误导为准）。

### 3.6 控制台向导：保存选项

**位置**：`RuleConfigWizard.start()` 中 `askTemplateOrInput(targetDir)` 返回非 null 之后、`writeRuleFile(rule, targetDir)` 预览之前，新增 `maybeSaveAsTemplate(rule)`（手动输入与套用模板两条路径都覆盖）：

1. 询问：`[选择] 是否将当前规则保存为自定义模板？(y/n, 回车=n): `——`n`/回车直接继续（既有流程输出不变）；
2. `y` → 先执行**保存前校验**（§3.4 同款：rule 序列化 → `MatchRuleLoader.fromJson` 非 null）；不合法 → `[失败] 当前规则不合法，无法保存为模板` 并**继续** `writeRuleFile`（不打断生成）；
3. 校验通过 → `[输入] 请输入模板名称 (回车=取消): `；回车/空 → `[取消] 未保存模板` 继续生成；名称与内置重名 → `[失败] 与内置模板重名，请换名` 重新输入（或回手动，以不产生死循环为准）；与自定义重名 → `[选择] 已存在同名模板「xx」，是否覆盖？(y/n, 回车=n): `；
4. 保存成功 → `[成功] 已保存自定义模板「名称」`（分类默认"自定义"，描述留空；控制台**不询问**分类/描述——保持最小交互）；
5. 保存失败（IO）→ `[失败] 保存模板失败: 原因`，**继续** `writeRuleFile`；
6. 删除/改名：**控制台不做**（保持控制台最小交互），由 GUI「管理自定义模板」入口覆盖；需求明确此边界，GUI/控制台共享同一存储文件，控制台保存的模板 GUI 可管理。

### 3.7 保存前校验（统一规则，两入口一致）

- 校验依据唯一：`rule` 序列化 JSON → `MatchRuleLoader.fromJson` 非 null（strategyType 与链步骤均已在注册表，与模板库加载校验、`writeRuleFile` 自校验同款）；
- 校验失败 → 不写文件、提示原因（GUI 弹窗 / 控制台 `[失败]` 行）；校验通过才允许写 `user-templates.json`；
- 文件层校验：写入前 id 唯一（自动生成保证）、name 非空；写入后不强制反读（写回用同一 ObjectMapper，与 `ConfigLoader.saveThemeTo` 一致，可选自校验）。

### 3.8 内置模板只读保护（明确边界）

- `rule-templates.json`（classpath）本批次**零改动**，加载/套用行为与 v0.1.15 一致；
- 库层 `deleteTemplate`/`renameTemplate` 仅作用于自定义集合，内置 id 一律拒绝（false）；
- GUI 管理对话框不展示内置模板（删除入口隐藏）；合并列表内置与自定义并存但**来源可辨**；
- 自定义模板保存时**不得覆盖/占用内置模板 id 与名称**（id 前缀 `custom-` 天然隔离；重名内置 → 拒绝）。

## 4. 交互与文案约定

- 文案风格沿用既有：GUI `[成功]` 绿 / `[失败]` 红 / `[警告]` 橙 / `[信息]` 青；表单内提示用 `warningLabel`（`UITheme` 色）；控制台 `[选择]/[输入]/[成功]/[失败]/[取消]` 前缀与既有一致；
- 所有文案中文，语气与既有 UI 一致；
- 自定义模板显示名带 `【自定义】`/`（自定义）`（GUI）与 `[自定义]`（控制台）标记，用户可一眼区分来源；
- 保存询问默认值 `n`（回车=n），与既有"是否套用模板"询问风格一致；
- 新对话框（保存输入、管理列表）必须走 `UITheme.apply()`/样式组件（工作区未提交的深色主题修复已覆盖 UIManager 通用键，实现直接受益）。

## 5. 非功能需求（NFR）

1. **零新增依赖**：只用已有 Jackson/SLF4J/Logback/Swing；`pom.xml` 不改；
2. **失败静默降级**：保存/删除/改名/加载任何失败分支不抛未捕获异常、不崩溃、不打断向导流程；自定义模板加载失败回退为空集，不影响内置模板与手动流程；
3. **Java 17 兼容**：无平台特有 API；路径操作用 `java.nio.file`；
4. **不污染内置资源**：自定义模板只写用户文件；`rule-templates.json` 零改动；
5. **发布包场景可用**：自定义模板存于工作目录 `templates/`（用户解压目录可写），jar 内置资源保持只读；主位置不可写时回退用户主目录；
6. **可测试性**：路径解析、校验、读写、合并、删除/改名、内置只读拒绝全部 headless 可测（包内钩子注入临时路径，`@TempDir` 隔离）；GUI 弹窗交互与深色主题可读性列为静态核对 + 用户实测（t4/t5）；
7. **零回归**：不套用/不保存时两向导行为与 v0.1.15 一致；内置模板加载/套用/`findById`/`copy` 既有测试全绿。

## 6. 实现约束（t2 必须遵守）

1. **不改** `RuleTemplate` 字段/`copy()` 语义；**不改** `MatchRuleLoader.fromJson` 签名语义；**不改** `RuleTemplateLibrary.loadAll()/findById()` 既有语义（findById 仅允许扩展"内置优先、自定义兜底"）；**不改** `rule-templates.json` 内容；
2. `RuleWizardForm`：`onOk()` 的规则组装逻辑**抽取**为 `buildRuleFromForm()`（保存按钮复用，禁止复制粘贴两份组装逻辑）；模板下拉改造为合并列表，套用改用显示名→模板映射；新增按钮不得重排既有区块顺序；
3. `RuleConfigWizard`：保存询问插在 `askTemplateOrInput` 返回后、`writeRuleFile` 预览前；`n`/回车路径输出与 v0.1.15 **逐字一致**；`Main.java` 无 diff（保存询问在向导内部）；
4. 库层文件写**照抄 `ConfigLoader.saveThemeTo` 模式**：读现有文件（去 BOM）→ 合并/替换 → 美化写回；失败仅记录（`LoggerUtil`），不抛异常；`templates/` 目录保存时自动创建；
5. 自定义模板 id 生成规则：`custom-` 前缀 + 时间戳/短随机，保证唯一；保存前必须显式做 `MatchRuleLoader` 校验；
6. 内置只读保护三层落实：管理对话框不展示内置（删除入口隐藏）+ 库层 `deleteTemplate/renameTemplate` 对内置 id 拒绝 + 保存时内置重名拒绝；
7. **同步更新** `RuleConfigWizardTest` 既有 3 个脚本（各追加一个不保存回答）+ 新增保存/复用闭环测试；`RuleTemplateLibraryTest` 既有断言保持全绿，新增自定义模板测试（同 `service` 包，`@TempDir`）；
8. 新增/改动文件中文注释、命名与既有风格一致；新增日志走 `LoggerUtil`，控制台向导交互输出沿用既有 `System.out.println` 风格（与 `inputRule`/模板询问一致）；无裸 `printStackTrace`；
9. 不触碰 `.agent-teams/`、`.dsh-*` 等运行时目录；工作区未提交的 `UITheme` 深色主题修复**不得回退**，集成时（t6）与船长协调一并提交；
10. 新对话框必须用 `UITheme` 样式组件，深色主题下可读（依赖工作区未提交的 UIManager 主题修复）。

## 7. 验收标准（t3 逐条执行；t4 审查引用；t5 审查引用；t6 实测引用）

### AC-1 编译与打包
- [ ] AC-1.1 `mvn -o compile` 通过（main 全量）；
- [ ] AC-1.2 `mvn -o test` 全绿（既有全部测试 + 本批次新增/更新测试）；
- [ ] AC-1.3 `mvn -o package -DskipTests` 成功产出可执行 jar。

### AC-2 存储位置解析（纯逻辑）
- [ ] AC-2.1 默认用户模板文件路径 = `<工作目录>/templates/user-templates.json`（工作目录 = `Paths.get(".")` 规范化绝对路径，与外部 config.json 同目录）；
- [ ] AC-2.2 保存时 `templates/` 目录不存在自动创建；
- [ ] AC-2.3 主位置不可写/创建失败时回退 `<user.home>/.frt/templates/user-templates.json`（包内钩子注入模拟）；
- [ ] AC-2.4 两级均失败 → 保存返回失败状态，不抛异常；
- [ ] AC-2.5 路径解析可经包内钩子/静态覆盖注入，测试用 `@TempDir` 隔离，不写真实工作目录。

### AC-3 保存（库层，纯逻辑）
- [ ] AC-3.1 `saveTemplate` 校验：id/name 非空、rule 非空；rule 序列化后经 `MatchRuleLoader.fromJson` 非 null 才允许写文件；校验失败返回 `INVALID_RULE`，**不写文件**；
- [ ] AC-3.2 成功保存后 `user-templates.json` 结构为 `{"templates":[...]}`，与内置同构，`RuleTemplate` 可反序列化，rule 与保存前一致（含 `strategyChain: []`）；
- [ ] AC-3.3 生成 id 以 `custom-` 开头且唯一（多次保存不冲突、不与内置 id 冲突）；
- [ ] AC-3.4 同 name 不同 id → 返回 `DUPLICATE_NAME`（不写）；`overwrite=true` → 更新同名模板全部字段（保持原 id）；
- [ ] AC-3.5 保存失败（只读路径/IO 异常）返回 `IO_ERROR`，不抛异常（测试或静态核对）；
- [ ] AC-3.6 自定义模板 name 与内置模板重名 → 拒绝保存（内置只读保护）。

### AC-4 加载与合并（纯逻辑）
- [ ] AC-4.1 `loadAllCustom()`：读取用户文件，逐条校验（同内置加载校验），坏文件/缺失 → 空列表 + `logWarn`，不抛异常；
- [ ] AC-4.2 `loadAllMerged()` = 内置（前）+ 自定义（后），顺序正确、数量正确、无重复 id；
- [ ] AC-4.3 `findById` 扩展：内置优先，未命中查自定义（`mc-mod-update` 仍命中内置；自定义 id 命中自定义；`no-such-template` 仍返回 null）；
- [ ] AC-4.4 自定义文件内 id 重复 → 保留第一条并 `logWarn`（不拖垮整体，与内置加载容错一致）。

### AC-5 删除/改名与内置只读（纯逻辑）
- [ ] AC-5.1 `deleteTemplate(customId)` 成功删除并写回，文件不再含该 id；返回 true；
- [ ] AC-5.2 `deleteTemplate(builtInId)` 返回 false（内置拒绝，文件不变）；不存在的 id 返回 false，不抛异常；
- [ ] AC-5.3 `renameTemplate(id, newName)`：name 更新、id 不变；新名非空且不与内置/其他自定义重名；失败返回 false；
- [ ] AC-5.4 `rule-templates.json`（classpath）内容零改动（git diff 核对）。

### AC-6 GUI 表单（静态核对 + 可测部分）
- [ ] AC-6.1 表单按钮行含「保存为模板」按钮（不重排既有区块），点击后组装当前表单规则；
- [ ] AC-6.2 保存前校验：规则不合法 → 弹窗提示，不写文件、不关闭表单；
- [ ] AC-6.3 保存成功 → 模板下拉刷新，新增项带自定义标记；提示成功，表单不关闭，可继续「确定生成」；
- [ ] AC-6.4 重名（自定义）→ 覆盖询问；重名（内置）→ 拒绝提示；
- [ ] AC-6.5 模板下拉 = 内置 + 自定义合并展示（自定义带标记），套用对两者一致生效（`applyRuleToForm` 深拷贝预填）；
- [ ] AC-6.6 「管理自定义模板」对话框仅列自定义模板；删除（确认后生效、列表与下拉刷新）；改名（非空且不冲突）；内置模板不展示（删除入口隐藏）；
- [ ] AC-6.7 新对话框使用 `UITheme` 样式，深色主题下可读（静态核对 + 实测）。

### AC-7 控制台向导保存（端到端脚本测试）
- [ ] AC-7.1 手动输入规则组装完成后询问 `[选择] 是否将当前规则保存为自定义模板？(y/n, 回车=n): `；`n`/回车 → 直接进入既有预览流程，输出与 v0.1.15 一致；
- [ ] AC-7.2 `y` → 校验通过 → 输入名称 → `[成功] 已保存自定义模板「名称」` → 继续 `writeRuleFile`（预览/确认/写入/自校验完整走通）；
- [ ] AC-7.3 套用内置模板后同样出现保存询问，可保存套用后的规则；
- [ ] AC-7.4 重名（自定义）→ 覆盖询问；重名（内置）→ `[失败]` 提示且不写文件；名称空/回车 → 取消保存，继续生成；
- [ ] AC-7.5 校验失败（构造非法规则场景）→ `[失败]` 提示，**继续** `writeRuleFile`，不崩溃；
- [ ] AC-7.6 保存失败（IO）→ `[失败]` 提示，继续生成流程；
- [ ] AC-7.7 `RuleConfigWizardTest` 既有 3 个测试脚本已追加不保存回答且全绿。

### AC-8 复用闭环（端到端，两入口共享存储）
- [ ] AC-8.1 控制台保存的自定义模板，下次控制台向导模板列表可见（带 `[自定义]` 标记，编号在最后）；
- [ ] AC-8.2 套用自定义模板 → 生成规则文件与保存时一致（`MatchRuleLoader` 解析非 null、字段逐项相等）；
- [ ] AC-8.3 GUI 保存的自定义模板在控制台可见，反之亦然（共享同一 `user-templates.json`）；
- [ ] AC-8.4 保存→删除→再保存同名模板全流程无残留、无异常。

### AC-9 静态核对（验证工程师/审查官）
- [ ] AC-9.1 无新增第三方依赖（`pom.xml` 未改）；
- [ ] AC-9.2 `Main.java` 无 diff（或仅注释级）；控制台菜单不回归；
- [ ] AC-9.3 `rule-templates.json` 无 diff；内置模板加载/套用行为不变；
- [ ] AC-9.4 新增路径日志走 `LoggerUtil`（库层）/向导既有 `System.out` 风格（控制台交互），无裸 `printStackTrace`/新增裸调试输出；
- [ ] AC-9.5 新增/改动文件中文注释、命名风格一致；`onOk` 组装逻辑已抽取复用（无两份组装代码）；
- [ ] AC-9.6 工作区未提交的 `UITheme` 深色主题修复未被回退（git diff 仍含该修复）。

### AC-10 零回归
- [ ] AC-10.1 不套用/不保存时两向导行为与 v0.1.15 一致（既有测试 + 静态核对）；
- [ ] AC-10.2 `RuleTemplateLibraryTest` 既有断言（7 个内置、findById、copy、loadFrom 容错）全绿；
- [ ] AC-10.3 全量 `mvn -o test` 绿（既有全部测试 + 本批次新增/更新）。

## 8. 测试需求（t2 实现，t3 执行）

1. **自定义模板库测试**（新增，`com.awei.frt.service` 包，`RuleTemplateLibraryTest` 同文件或新文件）：AC-2/3/4/5 全部——路径解析（包内钩子注入 `@TempDir`）、保存校验拒绝、写文件结构同构、重名/覆盖/内置重名拒绝、加载合并顺序、findById 扩展、删除/改名、内置删除拒绝、坏文件容错（复用 `loadFrom` 风格注入）；
2. **保存前校验测试**：`INVALID_RULE` 场景（构造未注册策略类型/链步骤未注册的规则）→ 拒绝且文件无变化（可并入 1）；
3. **`RuleConfigWizardTest` 更新**：既有 3 个脚本各追加一个 `"n"`/回车（不保存）；新增端到端：手动输入 → y 保存 → 输入名称 → 继续生成（AC-7.2）；套用模板后保存（AC-7.3）；重名覆盖（AC-7.4）；保存后下次套用闭环（AC-8.1/8.2）——用脚本化 `UserPrompter` + `@TempDir` 配置隔离（照既有模式）；
4. **GUI 表单可测部分**：若有表单结构测试先例则加按钮存在性断言，否则静态核对（t3）+ 用户实测（t4）；「保存为模板」的规则组装与校验逻辑若抽取为可测方法（推荐），加纯逻辑测试；
5. **既有测试零回归**：`mvn -o test` 全绿（现有全部测试 + 本批次新增/更新）。
6. GUI 交互项（保存弹窗、管理对话框、深色主题可读性）headless 无法自动化的部分，由 t3 静态核对 + t4 用户实测 + t5 审查覆盖。

## 9. 风险与回归关注点（t5 审查重点）

1. **`RuleConfigWizardTest` 脚本中断**：保存询问新增后既有脚本若漏加回答 → t3 全量测试红；实现与测试必须同批提交；
2. **`onOk` 与保存组装逻辑分叉**：若保存按钮复制一份组装代码，后续字段演进会漂移——必须抽取 `buildRuleFromForm()` 复用，审查逐行确认；
3. **内置只读保护穿透**：删除/改名 API 若作用于合并列表而非自定义集合，可能误删内置——库层必须按来源隔离（内置 id 拒绝），管理对话框不展示内置（双保险）；
4. **重名覆盖语义**：覆盖须保持原 id（引用不失效）；内置重名必须拒绝，不得把自定义模板"顶掉"内置展示；
5. **id 冲突**：`custom-` 前缀 + 时间戳/随机在极端并发下仍可能冲突——保存前做存在性检查，冲突则重新生成（或拒绝），审查确认；
6. **并发写文件**：GUI 保存/删除/改名与控制台保存共享同一文件，均为整文件读改写——本批次不新增并发入口（与 `ConfigLoader.saveThemeTo` 一致）；库层写方法建议加 `synchronized`（低成本防同窗口双操作），审查确认无新增并发写点；
7. **零回归输出**：控制台 `n`/回车路径输出必须与 v0.1.15 逐字一致；GUI 不套用模板时表单外观不变；
8. **工作目录污染**：测试若走默认路径会写真实工作目录 `templates/`——必须全部经包内钩子注入 `@TempDir`，审查核对测试代码；
9. **深色主题可读性**：新对话框若用裸 `JOptionPane`/默认组件，深色主题下可能黑字压深底——必须依赖工作区未提交的 `UITheme` UIManager 修复（t6 一并提交）并静态核对；
10. **文件损坏容错**：用户文件被手工改坏 → 加载空集 + logWarn，保存走"读失败按空集重建"（不得因坏文件导致永久无法保存）——审查确认保存路径对"现有文件解析失败"的降级处理。

## 10. 交付物（t6 集成范围）

- 本需求文档（`需求-v0.1.16-规则模板保存（自定义模板）.md`）；
- 代码（FR-1）：`RuleTemplateLibrary` 扩展（路径解析/`loadAllCustom`/`loadAllMerged`/`findById` 扩展/`saveTemplate`/`deleteTemplate`/`renameTemplate` + 包内钩子）；`RuleWizardForm`「保存为模板」按钮 + 管理对话框 + 下拉合并展示 + `buildRuleFromForm()` 抽取；`RuleConfigWizard` 保存询问 `maybeSaveAsTemplate`；可选 `ui/TemplateManageDialog.java`；
- 测试：自定义模板库测试、`RuleConfigWizardTest` 更新 + 保存/复用闭环新增；
- 提交信息建议：`feat(v0.1.16): 规则模板保存（自定义模板）`（最终由船长定夺；本地提交 dev，不推送；**含未提交的 UITheme 弹窗深色主题修复**，集成时与船长协调一并提交；tag/发布由船长处理）。
